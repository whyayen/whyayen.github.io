<?xml version="1.0" encoding="utf-8"?><?xml-stylesheet type="text/xsl" href="https://blog.giiin.dev/rss.xsl"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:dc="http://purl.org/dc/elements/1.1/">
  <channel>
    <atom:link href="https://blog.giiin.dev/rss.xml" rel="self" type="application/rss+xml"/>
    <title>Gin&amp;apos;s Blog</title>
    <link>https://blog.giiin.dev/</link>
    <description>Gin 的部落格</description>
    <language>zh-TW</language>
    <pubDate>Mon, 18 May 2026 09:17:21 GMT</pubDate>
    <lastBuildDate>Mon, 18 May 2026 09:17:21 GMT</lastBuildDate>
    <generator>@vuepress/plugin-feed</generator>
    <docs>https://validator.w3.org/feed/docs/rss2.html</docs>
    <category>Ruby on Rails</category>
    <category>CI</category>
    <category>ELK</category>
    <category>Miscullaneous</category>
    <category>AWS</category>
    <category>MySQL</category>
    <item>
      <title>測試</title>
      <link>https://blog.giiin.dev/posts/test.html</link>
      <guid>https://blog.giiin.dev/posts/test.html</guid>
      <source url="https://blog.giiin.dev/rss.xml">測試</source>
      <description>測試</description>
      <pubDate>Sun, 17 May 2026 00:00:00 GMT</pubDate>
      <content:encoded><![CDATA[
]]></content:encoded>
    </item>
    <item>
      <title>Ruby connection_pool 套件的實作原理</title>
      <link>https://blog.giiin.dev/posts/Ruby%20on%20Rails/2024/ruby_gem_connection_pool_intro.html</link>
      <guid>https://blog.giiin.dev/posts/Ruby%20on%20Rails/2024/ruby_gem_connection_pool_intro.html</guid>
      <source url="https://blog.giiin.dev/rss.xml">Ruby connection_pool 套件的實作原理</source>
      <description>最近剛在看一些 Ruby threads、GIL 的資料，看完想找個簡單不複雜的套件，看看是如何用 threads 實現一些功能，剛好 connection_pool 是一套常見且實現偏簡單的套件，用來建立一個 Connection Pool 來快取資料庫連線，避免每次重新建立連線，耗費大量時間，雖然它沒有開新 thread 的部分，但有一些利用到 M...</description>
      <category>Ruby on Rails</category>
      <pubDate>Fri, 31 May 2024 00:00:00 GMT</pubDate>
      <content:encoded><![CDATA[<p>最近剛在看一些 Ruby threads、GIL 的資料，看完想找個簡單不複雜的套件，看看是如何用 threads 實現一些功能，剛好 <a href="https://github.com/mperham/connection_pool" target="_blank" rel="noopener noreferrer">connection_pool</a> 是一套常見且實現偏簡單的套件，用來建立一個 Connection Pool 來快取資料庫連線，避免每次重新建立連線，耗費大量時間，雖然它沒有開新 thread 的部分，但有一些利用到 Mutex、ConditionVariable 的部分還是可以了解一下這麼做的原因為何。</p>
<p>主要的幾個檔案：</p>
<ul>
<li><code>connection_pool/timed_stack.rb</code>: 一個用來存放連線的 Stack 資料結構（裡面包含了連線建立、連線獲取方法）</li>
<li><code>connection_pool.rb</code>:  套件 Class 的主要方法接口</li>
<li><code>connection_pool/wrapper.rb</code>: 包覆一些方法，實現把 <code>ConnectionPool::Wrapper</code> 建立出來的物件可以直接用 <code>$redis.sadd</code> 之類的方法，不用在透過 <code>with</code> 把要執行的程式碼然後寫在 block 內</li>
</ul>
<h2>ConnectionPool::TimedStack Class</h2>
<p>這是一個 stack 的結構，主要用於存放已建立的 connections，並且透過用 Mutex 確保 <code>@que</code> 及其他變數不會因為多線程的讀寫導致 race condition。這裡主要的兩個方法 <code>pop</code> &amp; <code>push</code>，而 <code>pop</code> 主要的用途就是拿到 connection，使用完畢後可以在透過 <code>push</code> 方法存回 pool 堆疊裡面，所以越晚用完放回 stack 的 connection，下次會優先被拿出來用，這邊會用 stack 可能的原因我猜是：保持 connection 活躍，避免可能時長過久中斷需重新建立連線，或因為其他因素而變得不可用。</p>
<p>先從建構子開始看這些變數的用途：</p>
<div class="language-ruby line-numbers-mode" data-highlighter="prismjs" data-ext="rb"><pre><code class="language-ruby"><span class="line"><span class="token keyword">def</span> <span class="token method-definition"><span class="token function">initialize</span></span><span class="token punctuation">(</span>size <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">,</span> <span class="token operator">&amp;</span>block<span class="token punctuation">)</span></span>
<span class="line">  <span class="token variable">@create_block</span> <span class="token operator">=</span> block <span class="token comment"># 存放要執行建立連線的 block，例如 $redis.new</span></span>
<span class="line">  <span class="token variable">@created</span> <span class="token operator">=</span> <span class="token number">0</span> <span class="token comment"># 存放已建立的連線數量</span></span>
<span class="line">  <span class="token variable">@que</span> <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token punctuation">]</span> <span class="token comment"># 存放已建立的連線</span></span>
<span class="line">  <span class="token variable">@max</span> <span class="token operator">=</span> size <span class="token comment"># 最多能建立多少 connections</span></span>
<span class="line">  <span class="token variable">@mutex</span> <span class="token operator">=</span> <span class="token builtin">Thread</span><span class="token double-colon punctuation">::</span><span class="token class-name">Mutex</span><span class="token punctuation">.</span><span class="token keyword">new</span></span>
<span class="line">  <span class="token variable">@resource</span> <span class="token operator">=</span> <span class="token builtin">Thread</span><span class="token double-colon punctuation">::</span><span class="token class-name">ConditionVariable</span><span class="token punctuation">.</span><span class="token keyword">new</span></span>
<span class="line"><span class="token keyword">end</span></span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>push 方法就是將一個連線放入 stack（這裡先不管 shutdown_block 的場景）</p>
<div class="language-ruby line-numbers-mode" data-highlighter="prismjs" data-ext="rb"><pre><code class="language-ruby"><span class="line"><span class="token keyword">def</span> <span class="token method-definition"><span class="token function">push</span></span><span class="token punctuation">(</span>obj<span class="token punctuation">,</span> options <span class="token operator">=</span> <span class="token punctuation">{</span><span class="token punctuation">}</span><span class="token punctuation">)</span></span>
<span class="line">  <span class="token variable">@mutex</span><span class="token punctuation">.</span>synchronize <span class="token keyword">do</span></span>
<span class="line">    <span class="token keyword">if</span> <span class="token variable">@shutdown_block</span></span>
<span class="line">      <span class="token variable">@shutdown_block</span><span class="token punctuation">.</span>call<span class="token punctuation">(</span>obj<span class="token punctuation">)</span></span>
<span class="line">    <span class="token keyword">else</span></span>
<span class="line">      store_connection obj<span class="token punctuation">,</span> options</span>
<span class="line">    <span class="token keyword">end</span></span>
<span class="line"></span>
<span class="line">    <span class="token variable">@resource</span><span class="token punctuation">.</span>broadcast <span class="token comment"># 喚醒所有在 wait (sleep) 的 threads</span></span>
<span class="line">  <span class="token keyword">end</span></span>
<span class="line"><span class="token keyword">end</span></span>
<span class="line">alias_method <span class="token operator">:</span><span class="token operator">&lt;&lt;</span><span class="token punctuation">,</span> <span class="token symbol">:push</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># ...</span></span>
<span class="line"></span>
<span class="line"><span class="token keyword">def</span> <span class="token method-definition"><span class="token function">store_connection</span></span><span class="token punctuation">(</span>obj<span class="token punctuation">,</span> options <span class="token operator">=</span> <span class="token keyword">nil</span><span class="token punctuation">)</span></span>
<span class="line">  <span class="token variable">@que</span><span class="token punctuation">.</span>push obj</span>
<span class="line"><span class="token keyword">end</span></span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>pop 方法就複雜一點，主要在做的事情：</p>
<ol>
<li>如果已經有之前建立的 connection（stack 不是空的），就直接 pop stack 的 connection</li>
<li>如果 stack 是空的則嘗試建立一個 connection 並返回</li>
<li>檢查是否已經 Timeout 是的話就 raise TimeoutError</li>
<li>讓 thread 先進入 sleep 待有訊號喚醒時（<code>resource.broadcast</code> 或 <code>resource.signal</code>），才又重新回到第一步的流程在一次，使用 ConditionVariable 是因為上述的情況就是目前拿不到或產不了 connection，沒必要一直跑迴圈浪費資源，先讓 thread 進入睡眠</li>
</ol>
<div class="language-ruby line-numbers-mode" data-highlighter="prismjs" data-ext="rb"><pre><code class="language-ruby"><span class="line"><span class="token keyword">def</span> <span class="token method-definition"><span class="token function">pop</span></span><span class="token punctuation">(</span>timeout <span class="token operator">=</span> <span class="token number">0.5</span><span class="token punctuation">,</span> options <span class="token operator">=</span> <span class="token punctuation">{</span><span class="token punctuation">}</span><span class="token punctuation">)</span></span>
<span class="line">  options<span class="token punctuation">,</span> timeout <span class="token operator">=</span> timeout<span class="token punctuation">,</span> <span class="token number">0.5</span> <span class="token keyword">if</span> <span class="token builtin">Hash</span> <span class="token operator">===</span> timeout</span>
<span class="line">  timeout <span class="token operator">=</span> options<span class="token punctuation">.</span>fetch <span class="token symbol">:timeout</span><span class="token punctuation">,</span> timeout</span>
<span class="line"></span>
<span class="line">  deadline <span class="token operator">=</span> current_time <span class="token operator">+</span> timeout</span>
<span class="line">  <span class="token variable">@mutex</span><span class="token punctuation">.</span>synchronize <span class="token keyword">do</span></span>
<span class="line">    loop <span class="token keyword">do</span></span>
<span class="line">      <span class="token keyword">raise</span> ConnectionPool<span class="token double-colon punctuation">::</span>PoolShuttingDownError <span class="token keyword">if</span> <span class="token variable">@shutdown_block</span></span>
<span class="line">      <span class="token comment"># 當 stack 不是空的時候，直接返回之前建立的 connection</span></span>
<span class="line">      <span class="token keyword">return</span> fetch_connection<span class="token punctuation">(</span>options<span class="token punctuation">)</span> <span class="token keyword">if</span> connection_stored<span class="token operator">?</span><span class="token punctuation">(</span>options<span class="token punctuation">)</span></span>
<span class="line"></span>
<span class="line">      <span class="token comment"># 沒有 connection 的時候，嘗試建立一個</span></span>
<span class="line">      connection <span class="token operator">=</span> try_create<span class="token punctuation">(</span>options<span class="token punctuation">)</span></span>
<span class="line">      <span class="token keyword">return</span> connection <span class="token keyword">if</span> connection</span>
<span class="line"></span>
<span class="line">      to_wait <span class="token operator">=</span> deadline <span class="token operator">-</span> current_time</span>
<span class="line">      <span class="token comment"># 如果已經超過時間仍還沒法獲取/建立連線的時候，則拋出一個錯誤</span></span>
<span class="line">      <span class="token keyword">raise</span> ConnectionPool<span class="token double-colon punctuation">::</span>TimeoutError<span class="token punctuation">,</span> <span class="token string-literal"><span class="token string">"Waited </span><span class="token interpolation"><span class="token delimiter punctuation">#{</span><span class="token content">timeout</span><span class="token delimiter punctuation">}</span></span><span class="token string"> sec, </span><span class="token interpolation"><span class="token delimiter punctuation">#{</span><span class="token content">length</span><span class="token delimiter punctuation">}</span></span><span class="token string">/</span><span class="token interpolation"><span class="token delimiter punctuation">#{</span><span class="token content"><span class="token variable">@max</span></span><span class="token delimiter punctuation">}</span></span><span class="token string"> available"</span></span> <span class="token keyword">if</span> to_wait <span class="token operator">&lt;=</span> <span class="token number">0</span></span>
<span class="line">      <span class="token comment"># 讓 thread 進入 sleep 並釋放鎖，待 @resource.signal 或 @resource.broadcast 時才會喚醒 thread 並重新獲取鎖</span></span>
<span class="line">      <span class="token comment"># 在往下執行，以這裡就是又進迴圈重新獲取/建立 connection</span></span>
<span class="line">      <span class="token comment"># 按照 connection_pool 的設計是先 pop 然後連線用完在 push 進 stack，一次執行就是一對 pop/push 的動作</span></span>
<span class="line">      <span class="token comment"># 這裡用 ConditionVariable 是因為只有 stack 有新元素的時候，才會需要在執行，不然就是當前情況仍沒有辦法獲取/建立新連線</span></span>
<span class="line">      <span class="token variable">@resource</span><span class="token punctuation">.</span>wait<span class="token punctuation">(</span><span class="token variable">@mutex</span><span class="token punctuation">,</span> to_wait<span class="token punctuation">)</span></span>
<span class="line">    <span class="token keyword">end</span></span>
<span class="line">  <span class="token keyword">end</span></span>
<span class="line"><span class="token keyword">end</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># 返回 stack 最上面的連線</span></span>
<span class="line"><span class="token keyword">def</span> <span class="token method-definition"><span class="token function">fetch_connection</span></span><span class="token punctuation">(</span>options <span class="token operator">=</span> <span class="token keyword">nil</span><span class="token punctuation">)</span></span>
<span class="line">  <span class="token variable">@que</span><span class="token punctuation">.</span>pop</span>
<span class="line"><span class="token keyword">end</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># 當已建立的連線還沒到達最大值的時候，執行 create_block，並且將已建立連線數加 1，然後返回物件本身</span></span>
<span class="line"><span class="token keyword">def</span> <span class="token method-definition"><span class="token function">try_create</span></span><span class="token punctuation">(</span>options <span class="token operator">=</span> <span class="token keyword">nil</span><span class="token punctuation">)</span></span>
<span class="line">  <span class="token keyword">unless</span> <span class="token variable">@created</span> <span class="token operator">==</span> <span class="token variable">@max</span></span>
<span class="line">    object <span class="token operator">=</span> <span class="token variable">@create_block</span><span class="token punctuation">.</span>call</span>
<span class="line">    <span class="token variable">@created</span> <span class="token operator">+=</span> <span class="token number">1</span></span>
<span class="line">    object</span>
<span class="line">  <span class="token keyword">end</span></span>
<span class="line"><span class="token keyword">end</span></span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2>ConnectionPool Class</h2>
<p>而 ConnectionPool 就是一個套件的接口方法，沒做什麼太複雜的事情，這裡我們就只看幾個主要的方法：<code>with</code>、<code>checkout</code>、<code>checkin</code> 。</p>
<p>主要存放資料的變數：</p>
<div class="language-ruby line-numbers-mode" data-highlighter="prismjs" data-ext="rb"><pre><code class="language-ruby"><span class="line"><span class="token keyword">def</span> <span class="token method-definition"><span class="token function">initialize</span></span><span class="token punctuation">(</span>options <span class="token operator">=</span> <span class="token punctuation">{</span><span class="token punctuation">}</span><span class="token punctuation">,</span> <span class="token operator">&amp;</span>block<span class="token punctuation">)</span></span>
<span class="line">  <span class="token comment"># ...</span></span>
<span class="line"></span>
<span class="line">  <span class="token variable">@size</span> <span class="token operator">=</span> <span class="token builtin">Integer</span><span class="token punctuation">(</span>options<span class="token punctuation">.</span>fetch<span class="token punctuation">(</span><span class="token symbol">:size</span><span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token comment"># 連線池的最大連線數量</span></span>
<span class="line">  <span class="token variable">@timeout</span> <span class="token operator">=</span> options<span class="token punctuation">.</span>fetch<span class="token punctuation">(</span><span class="token symbol">:timeout</span><span class="token punctuation">)</span> <span class="token comment"># Timeout 時間</span></span>
<span class="line">  <span class="token variable">@auto_reload_after_fork</span> <span class="token operator">=</span> options<span class="token punctuation">.</span>fetch<span class="token punctuation">(</span><span class="token symbol">:auto_reload_after_fork</span><span class="token punctuation">)</span></span>
<span class="line"></span>
<span class="line">  <span class="token variable">@available</span> <span class="token operator">=</span> <span class="token class-name">TimedStack</span><span class="token punctuation">.</span><span class="token keyword">new</span><span class="token punctuation">(</span><span class="token variable">@size</span><span class="token punctuation">,</span> <span class="token operator">&amp;</span>block<span class="token punctuation">)</span> <span class="token comment"># TimedStack 物件用來建立/獲取連線</span></span>
<span class="line">  <span class="token variable">@key</span> <span class="token operator">=</span> <span class="token symbol">:"pool-#{@available.object_id}"</span> <span class="token comment"># TimedStack 物件對應的 連線池</span></span>
<span class="line">  <span class="token variable">@key_count</span> <span class="token operator">=</span> <span class="token symbol">:"pool-#{@available.object_id}-count"</span> <span class="token comment"># TimedStack 物件對應的 連線數量</span></span>
<span class="line"></span>
<span class="line">  <span class="token comment"># ...</span></span>
<span class="line"><span class="token keyword">end</span></span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>with</code> 方法先呼叫 <code>checkout</code> 拿到 connection，可以看到這裡用 <code>Thread.handle_interrupt(Exception ⇒ :never)</code> 包起來是因為希望這個區塊中的程式發生異常的時候被延遲到區塊結束在處理，確保 connection 正常釋放回到 connection pool 中。</p>
<div class="language-ruby line-numbers-mode" data-highlighter="prismjs" data-ext="rb"><pre><code class="language-ruby"><span class="line"><span class="token keyword">def</span> <span class="token method-definition"><span class="token function">with</span></span><span class="token punctuation">(</span>options <span class="token operator">=</span> <span class="token punctuation">{</span><span class="token punctuation">}</span><span class="token punctuation">)</span></span>
<span class="line">  <span class="token builtin">Thread</span><span class="token punctuation">.</span>handle_interrupt<span class="token punctuation">(</span><span class="token builtin">Exception</span> <span class="token operator">=></span> <span class="token symbol">:never</span><span class="token punctuation">)</span> <span class="token keyword">do</span></span>
<span class="line">    conn <span class="token operator">=</span> checkout<span class="token punctuation">(</span>options<span class="token punctuation">)</span></span>
<span class="line"></span>
<span class="line">    <span class="token comment"># ...</span></span>
<span class="line">  <span class="token keyword">end</span></span>
<span class="line"><span class="token keyword">end</span></span>
<span class="line">alias_method <span class="token symbol">:then</span><span class="token punctuation">,</span> <span class="token symbol">:with</span></span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>而這裡又用 <code>Thread.handle_interrupt(Exception =&gt; :immediate)</code> 包覆則是因為當異常發生時馬上處理，不要延遲到區塊執行完畢延遲處理，而 ensure 則是發生異常也要把 connection 放回 connection pool 中。</p>
<div class="language-ruby line-numbers-mode" data-highlighter="prismjs" data-ext="rb"><pre><code class="language-ruby"><span class="line"><span class="token keyword">begin</span></span>
<span class="line">  <span class="token builtin">Thread</span><span class="token punctuation">.</span>handle_interrupt<span class="token punctuation">(</span><span class="token builtin">Exception</span> <span class="token operator">=></span> <span class="token symbol">:immediate</span><span class="token punctuation">)</span> <span class="token keyword">do</span></span>
<span class="line">    <span class="token keyword">yield</span> conn</span>
<span class="line">  <span class="token keyword">end</span></span>
<span class="line"><span class="token keyword">ensure</span></span>
<span class="line">  checkin</span>
<span class="line"><span class="token keyword">end</span></span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>checkout</code> 方法其實就是從 connection pool 中拿到 connection 並把</p>
<ul>
<li>
<p><code>@key_count + 1</code> 存回 current thread local variable</p>
</li>
<li>
<p>把拿到的 connection 物件存回 current thread local variable</p>
</li>
</ul>
<div class="language-ruby line-numbers-mode" data-highlighter="prismjs" data-ext="rb"><pre><code class="language-ruby"><span class="line"><span class="token keyword">def</span> <span class="token method-definition"><span class="token function">checkout</span></span><span class="token punctuation">(</span>options <span class="token operator">=</span> <span class="token punctuation">{</span><span class="token punctuation">}</span><span class="token punctuation">)</span></span>
<span class="line">  <span class="token keyword">if</span> <span class="token double-colon punctuation">::</span><span class="token builtin">Thread</span><span class="token punctuation">.</span>current<span class="token punctuation">[</span><span class="token variable">@key</span><span class="token punctuation">]</span></span>
<span class="line">    <span class="token double-colon punctuation">::</span><span class="token builtin">Thread</span><span class="token punctuation">.</span>current<span class="token punctuation">[</span><span class="token variable">@key_count</span><span class="token punctuation">]</span> <span class="token operator">+=</span> <span class="token number">1</span></span>
<span class="line">    <span class="token double-colon punctuation">::</span><span class="token builtin">Thread</span><span class="token punctuation">.</span>current<span class="token punctuation">[</span><span class="token variable">@key</span><span class="token punctuation">]</span></span>
<span class="line">  <span class="token keyword">else</span></span>
<span class="line">    <span class="token double-colon punctuation">::</span><span class="token builtin">Thread</span><span class="token punctuation">.</span>current<span class="token punctuation">[</span><span class="token variable">@key_count</span><span class="token punctuation">]</span> <span class="token operator">=</span> <span class="token number">1</span></span>
<span class="line">    <span class="token double-colon punctuation">::</span><span class="token builtin">Thread</span><span class="token punctuation">.</span>current<span class="token punctuation">[</span><span class="token variable">@key</span><span class="token punctuation">]</span> <span class="token operator">=</span> <span class="token variable">@available</span><span class="token punctuation">.</span>pop<span class="token punctuation">(</span>options<span class="token punctuation">[</span><span class="token symbol">:timeout</span><span class="token punctuation">]</span> <span class="token operator">||</span> <span class="token variable">@timeout</span><span class="token punctuation">)</span></span>
<span class="line">  <span class="token keyword">end</span></span>
<span class="line"><span class="token keyword">end</span></span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>checkin</code> 方法則是把 connection 物件（<code>TimedStack.pop</code> 拿到的）存回去 connection pool 中</p>
<div class="language-ruby line-numbers-mode" data-highlighter="prismjs" data-ext="rb"><pre><code class="language-ruby"><span class="line"><span class="token keyword">def</span> <span class="token method-definition"><span class="token function">checkin</span></span><span class="token punctuation">(</span><span class="token symbol">force</span><span class="token operator">:</span> <span class="token boolean">false</span><span class="token punctuation">)</span></span>
<span class="line">  <span class="token keyword">if</span> <span class="token double-colon punctuation">::</span><span class="token builtin">Thread</span><span class="token punctuation">.</span>current<span class="token punctuation">[</span><span class="token variable">@key</span><span class="token punctuation">]</span></span>
<span class="line">    <span class="token keyword">if</span> <span class="token double-colon punctuation">::</span><span class="token builtin">Thread</span><span class="token punctuation">.</span>current<span class="token punctuation">[</span><span class="token variable">@key_count</span><span class="token punctuation">]</span> <span class="token operator">==</span> <span class="token number">1</span> <span class="token operator">||</span> force</span>
<span class="line">      <span class="token variable">@available</span><span class="token punctuation">.</span>push<span class="token punctuation">(</span><span class="token double-colon punctuation">::</span><span class="token builtin">Thread</span><span class="token punctuation">.</span>current<span class="token punctuation">[</span><span class="token variable">@key</span><span class="token punctuation">]</span><span class="token punctuation">)</span></span>
<span class="line">      <span class="token double-colon punctuation">::</span><span class="token builtin">Thread</span><span class="token punctuation">.</span>current<span class="token punctuation">[</span><span class="token variable">@key</span><span class="token punctuation">]</span> <span class="token operator">=</span> <span class="token keyword">nil</span></span>
<span class="line">      <span class="token double-colon punctuation">::</span><span class="token builtin">Thread</span><span class="token punctuation">.</span>current<span class="token punctuation">[</span><span class="token variable">@key_count</span><span class="token punctuation">]</span> <span class="token operator">=</span> <span class="token keyword">nil</span></span>
<span class="line">    <span class="token keyword">else</span></span>
<span class="line">      <span class="token double-colon punctuation">::</span><span class="token builtin">Thread</span><span class="token punctuation">.</span>current<span class="token punctuation">[</span><span class="token variable">@key_count</span><span class="token punctuation">]</span> <span class="token operator">-=</span> <span class="token number">1</span></span>
<span class="line">    <span class="token keyword">end</span></span>
<span class="line">  <span class="token keyword">elsif</span> <span class="token operator">!</span>force</span>
<span class="line">    <span class="token keyword">raise</span> ConnectionPool<span class="token double-colon punctuation">::</span>Error<span class="token punctuation">,</span> <span class="token string-literal"><span class="token string">"no connections are checked out"</span></span></span>
<span class="line">  <span class="token keyword">end</span></span>
<span class="line"></span>
<span class="line">  <span class="token keyword">nil</span></span>
<span class="line"><span class="token keyword">end</span></span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2>ConnectionPool::Wrapper Class</h2>
<p>透過 <code>method_missing</code> 讓定義在 Class 內的方法可以透過 <code>connection.send</code> 去執行，而不需要每次都拿 <code>ConnectionPool.new</code> 建立的 instance 丟到 with 方法的 block 中執行。</p>
<div class="language-ruby line-numbers-mode" data-highlighter="prismjs" data-ext="rb"><pre><code class="language-ruby"><span class="line"><span class="token keyword">def</span> <span class="token method-definition"><span class="token function">initialize</span></span><span class="token punctuation">(</span>options <span class="token operator">=</span> <span class="token punctuation">{</span><span class="token punctuation">}</span><span class="token punctuation">,</span> <span class="token operator">&amp;</span>block<span class="token punctuation">)</span></span>
<span class="line">  <span class="token variable">@pool</span> <span class="token operator">=</span> options<span class="token punctuation">.</span>fetch<span class="token punctuation">(</span><span class="token symbol">:pool</span><span class="token punctuation">)</span> <span class="token punctuation">{</span> <span class="token double-colon punctuation">::</span><span class="token class-name">ConnectionPool</span><span class="token punctuation">.</span><span class="token keyword">new</span><span class="token punctuation">(</span>options<span class="token punctuation">,</span> <span class="token operator">&amp;</span>block<span class="token punctuation">)</span> <span class="token punctuation">}</span></span>
<span class="line"><span class="token keyword">end</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># ...</span></span>
<span class="line"></span>
<span class="line"><span class="token keyword">def</span> <span class="token method-definition"><span class="token function">method_missing</span></span><span class="token punctuation">(</span>name<span class="token punctuation">,</span> <span class="token operator">*</span>args<span class="token punctuation">,</span> <span class="token operator">**</span>kwargs<span class="token punctuation">,</span> <span class="token operator">&amp;</span>block<span class="token punctuation">)</span></span>
<span class="line">  with <span class="token keyword">do</span> <span class="token operator">|</span>connection<span class="token operator">|</span></span>
<span class="line">    connection<span class="token punctuation">.</span>send<span class="token punctuation">(</span>name<span class="token punctuation">,</span> <span class="token operator">*</span>args<span class="token punctuation">,</span> <span class="token operator">**</span>kwargs<span class="token punctuation">,</span> <span class="token operator">&amp;</span>block<span class="token punctuation">)</span></span>
<span class="line">  <span class="token keyword">end</span></span>
<span class="line"><span class="token keyword">end</span></span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>後續就不用每次 call <code>with</code> 方法，然後把想執行的程式碼丟到 block 中了。</p>
<div class="language-ruby line-numbers-mode" data-highlighter="prismjs" data-ext="rb"><pre><code class="language-ruby"><span class="line"><span class="token variable">$redis</span> <span class="token operator">=</span> ConnectionPool<span class="token double-colon punctuation">::</span><span class="token class-name">Wrapper</span><span class="token punctuation">.</span><span class="token keyword">new</span><span class="token punctuation">(</span><span class="token symbol">size</span><span class="token operator">:</span> <span class="token number">5</span><span class="token punctuation">,</span> <span class="token symbol">timeout</span><span class="token operator">:</span> <span class="token number">3</span><span class="token punctuation">)</span> <span class="token punctuation">{</span> <span class="token class-name">Redis</span><span class="token punctuation">.</span><span class="token keyword">new</span> <span class="token punctuation">}</span></span>
<span class="line"><span class="token variable">$redis</span><span class="token punctuation">.</span>sadd<span class="token punctuation">(</span><span class="token string-literal"><span class="token string">'foo'</span></span><span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">)</span></span>
<span class="line"><span class="token variable">$redis</span><span class="token punctuation">.</span>smembers<span class="token punctuation">(</span><span class="token string-literal"><span class="token string">'foo'</span></span><span class="token punctuation">)</span></span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div>]]></content:encoded>
    </item>
    <item>
      <title>Circle CI 自動 skip GitHub Draft Pull Request</title>
      <link>https://blog.giiin.dev/posts/CI/2021/circle_ci_cancel_github_draft_pr.html</link>
      <guid>https://blog.giiin.dev/posts/CI/2021/circle_ci_cancel_github_draft_pr.html</guid>
      <source url="https://blog.giiin.dev/rss.xml">Circle CI 自動 skip GitHub Draft Pull Request</source>
      <description>前言
由於暫時不打算在 CI 工具上付費，基本上免費額度已經夠用，只是如果每 push 一次，就會跑一次 CI，好處是能當下看到是否有測試未通過，不過礙於測試一次跑完要耗費 150 credits 以上，免費額度大概 2 天就沒了，但對我們而言 Draft PR 是還在開發中，所以只要最終整合、驗收時測試有跑過就好。
實現方式
我們使用 Circle ...</description>
      <category>CI</category>
      <pubDate>Mon, 26 Apr 2021 00:00:00 GMT</pubDate>
      <content:encoded><![CDATA[<h2>前言</h2>
<p>由於暫時不打算在 CI 工具上付費，基本上免費額度已經夠用，只是如果每 push 一次，就會跑一次 CI，好處是能當下看到是否有測試未通過，不過礙於測試一次跑完要耗費 150 credits 以上，免費額度大概 2 天就沒了，但對我們而言 Draft PR 是還在開發中，所以只要最終整合、驗收時測試有跑過就好。</p>
<h2>實現方式</h2>
<p>我們使用 Circle CI 本身並沒有直接支援 pattern 或設定的方式，可以直接跳過 GitHub Draft PR 不跑測試，所以找了一些解決方案，最終找到 <a href="https://circleci.com/developer/orbs/orb/artsy/skip-wip-ci" target="_blank" rel="noopener noreferrer">artsy/skip-wip-ci</a> orb，不過有一些缺點就是要餵 <code>CIRCLE_PROJECT_USERNAME</code>、<code>CIRCLE_PR_REPONAME</code>、<code>CIRCLE_PR_NUMBER</code>、<code>CIRCLE_BUILD_NUM</code> 這些環境變數，有點麻煩，但其實這些環境變數已經是 <a href="https://circleci.com/docs/2.0/env-vars/#built-in-environment-variables" target="_blank" rel="noopener noreferrer">Built-in environment variables</a>，所以理應可以只設定 <code>GITHUB_TOKEN</code>、<code>CIRCLE_TOKEN</code> 就可以跑這個 orb。</p>
<p>照著上面的想法，剛好那個 orb 是 MIT LICENSE 可以直接 fork 來改，於是大部分都是基於 <a href="https://circleci.com/developer/orbs/orb/artsy/skip-wip-ci" target="_blank" rel="noopener noreferrer">artsy/skip-wip-ci</a> orb 的實作，稍微做了一些修正，流程如下：</p>
<ol>
<li>透過 Circle CI Built-in 環境變數，拿到 <code>CIRCLE_PROJECT_USERNAME</code>、<code>CIRCLE_PROJECT_REPONAME</code>、以及 PR Number</li>
</ol>
<div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">https://api.github.com/repos/${CIRCLE_PROJECT_USERNAME}/${CIRCLE_PROJECT_REPONAME}/pulls/${CIRCLE_PULL_REQUEST##*/}</span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div></div></div><ol start="2">
<li>接著去打 API 檢查 PR 狀態是不是 Draft</li>
<li>如果是 Draft，則去打 Circle CI API 取消整個 Workflow</li>
</ol>
<div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code class="language-bash"><span class="line"><span class="token function">curl</span> <span class="token parameter variable">-X</span> POST https://circleci.com/api/v2/workflow/<span class="token variable">${CIRCLE_WORKFLOW_ID}</span>/cancel <span class="token parameter variable">-H</span> <span class="token string">'Accept: application/json'</span> <span class="token parameter variable">-u</span> <span class="token string">"<span class="token variable">${CIRCLE_TOKEN}</span>:"</span></span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div></div></div><h2>bye-github-draft</h2>
<p><a href="https://github.com/whyayen/bye-github-draft" target="_blank" rel="noopener noreferrer">bye-github-draft</a> 是基於 <a href="https://circleci.com/developer/orbs/orb/artsy/skip-wip-ci" target="_blank" rel="noopener noreferrer">artsy/skip-wip-ci</a> fork 出來修改的版本，使用前必須先：</p>
<ol>
<li>確保 Organization Settings 有開啟 <strong>Allow Uncertified Orbs</strong> 設定</li>
<li>去 User settings 建立 <strong>Personal API Tokens</strong>（<strong>Circle CI API v2 不支援 Project Token</strong>）</li>
<li>到 Circle CI Project Settings 新增環境變數，名稱：<code>CIRCLE_TOKEN</code>，值為剛剛建立的 Personal API Token</li>
<li>到 GitHub 建立 Personal Access Token，需包含 <code>repo</code> 權限</li>
<li>到 Circle CI Project Settings 新增環境變數，名稱：<code>GITHUB_TOKEN</code>，值為剛剛戀的 GitHub Personal Access Token</li>
<li>修改你的 <code>config.yml</code></li>
</ol>
<div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml"><pre><code class="language-yaml"><span class="line"><span class="token key atrule">version</span><span class="token punctuation">:</span> <span class="token number">2.1</span> <span class="token comment"># 一定要是 2.1 版，2.0 過不了</span></span>
<span class="line"><span class="token key atrule">orbs</span><span class="token punctuation">:</span></span>
<span class="line">  <span class="token key atrule">bye-github-draft</span><span class="token punctuation">:</span> whyayen/bye<span class="token punctuation">-</span>github<span class="token punctuation">-</span>draft@0.0.1</span>
<span class="line"></span>
<span class="line"><span class="token comment"># 你的 jobs/workflows/commands</span></span>
<span class="line"></span>
<span class="line"><span class="token key atrule">workflows</span><span class="token punctuation">:</span></span>
<span class="line">  <span class="token key atrule">your_workflow</span><span class="token punctuation">:</span></span>
<span class="line">    <span class="token key atrule">jobs</span><span class="token punctuation">:</span></span>
<span class="line">      <span class="token punctuation">-</span> bye<span class="token punctuation">-</span>github<span class="token punctuation">-</span>draft/check<span class="token punctuation">-</span>skippable<span class="token punctuation">-</span>pr</span>
<span class="line">      <span class="token comment"># 確保你的 job 跑在 check-skippable-pr 之後</span></span>
<span class="line">      <span class="token punctuation">-</span> <span class="token key atrule">your_job</span><span class="token punctuation">:</span></span>
<span class="line">          <span class="token key atrule">requires</span><span class="token punctuation">:</span></span>
<span class="line">            <span class="token punctuation">-</span> bye<span class="token punctuation">-</span>github<span class="token punctuation">-</span>draft/check<span class="token punctuation">-</span>skippable<span class="token punctuation">-</span>pr</span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2>總結</h2>
<p>照著上述使用 orb 的步驟，應該是蠻輕鬆簡單可以做到 Circle CI skip GitHub Draft PR，不過缺點是當你把 Draft PR 轉為一般 PR 後，沒有 push 任何 commit，就等於整個 PR 都沒跑過測試，其實蠻容易讓程式碼變髒、測試沒有維護等問題出現，其次是它仍然會消耗約 2 ~ 3 credits，不過如果暫時沒有經費花費在 CI 上，也許這也是一種做法，但是團隊內應該要討論 Merge 之前，要如何確保程式通過測試、跑測試的機制。</p>
]]></content:encoded>
    </item>
    <item>
      <title>讓 elastic-model 的 msearch 也有 records 方法</title>
      <link>https://blog.giiin.dev/posts/Ruby%20on%20Rails/2021/elastic_model_multi_search_active_records.html</link>
      <guid>https://blog.giiin.dev/posts/Ruby%20on%20Rails/2021/elastic_model_multi_search_active_records.html</guid>
      <source url="https://blog.giiin.dev/rss.xml">讓 elastic-model 的 msearch 也有 records 方法</source>
      <description>前言
在 Rails 內要與 Elasticsearch 服務整合成 ActiveRecord::Base 的話，我們會使用 elastic-model 來對 index 做 search，透過 Model 的 __elasticsearch__  proxy 可對單一 index 做 search，且可透過 records 方法將 Elasticse...</description>
      <category>Ruby on Rails</category>
      <category>ELK</category>
      <pubDate>Sat, 10 Apr 2021 00:00:00 GMT</pubDate>
      <content:encoded><![CDATA[<h2>前言</h2>
<p>在 Rails 內要與 Elasticsearch 服務整合成 ActiveRecord::Base 的話，我們會使用 <a href="https://github.com/elastic/elasticsearch-rails/tree/master/elasticsearch-model" target="_blank" rel="noopener noreferrer">elastic-model</a> 來對 index 做 search，透過 Model 的 <code>__elasticsearch__ </code> proxy 可對單一 index 做 search，且可透過 <code>records</code> 方法將 Elasticsearch 的搜尋結果轉換為 ActiveRecords，然而使用 <code>msearch</code> 同時搜尋多個 index 時就不支援此方法了。</p>
<h2>單 index 搜尋</h2>
<p>我們先來看只對單一 index 搜尋時，要如何透過 <code>elastic-model</code> 來達成，首先必須在 Model 進行設定：</p>
<div class="language-ruby line-numbers-mode" data-highlighter="prismjs" data-ext="rb"><pre><code class="language-ruby"><span class="line"><span class="token keyword">require</span> <span class="token string-literal"><span class="token string">'elasticsearch/model'</span></span></span>
<span class="line"></span>
<span class="line"><span class="token keyword">class</span> <span class="token class-name">Article</span> <span class="token operator">&lt;</span> ActiveRecord<span class="token double-colon punctuation">::</span>Base</span>
<span class="line">  <span class="token keyword">include</span> Elasticsearch<span class="token double-colon punctuation">::</span>Model</span>
<span class="line"><span class="token keyword">end</span></span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>如果需要一些額外設定，例如將 Model 對應到特定 index，可以參考 <a href="https://github.com/elastic/elasticsearch-rails/tree/master/elasticsearch-model#index-configuration" target="_blank" rel="noopener noreferrer">Index Configuration</a> 的相關說明。</p>
<p>一般 Model 會有一個 <code>__elasticsearch__</code> 的 proxy，詳細可以參考 <a href="https://github.com/elastic/elasticsearch-rails/tree/master/elasticsearch-model#the-elasticsearch-client" target="_blank" rel="noopener noreferrer">The <strong>elasticsearch</strong> Proxy</a> 小節，有了這個 proxy 便可進行 index 的搜尋等行為：</p>
<div class="language-ruby line-numbers-mode" data-highlighter="prismjs" data-ext="rb"><pre><code class="language-ruby"><span class="line">Article<span class="token punctuation">.</span>__elasticsearch__<span class="token punctuation">.</span>search<span class="token punctuation">(</span><span class="token string-literal"><span class="token string">'fox'</span></span><span class="token punctuation">)</span></span>
<span class="line">Article<span class="token punctuation">.</span>search<span class="token punctuation">(</span><span class="token string-literal"><span class="token string">'fox'</span></span><span class="token punctuation">)</span></span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div></div></div><p>個人還是比較建議用第一種 <code>__elasticsearch__</code> 方式去呼叫 <code>search</code> 方法，因為 <code>search</code> 方法可能已經存在 Model 本身作為其他用途了，如果用 <code>Model.search</code> 遇到 <code>search</code> 方法被用走，又要改成用 <code>__elasticsearch__</code> proxy 呼叫時，就又顯得不統一、凌亂。</p>
<p>如果今天有多個 Elasticsearch Host，勢必意味著需要多個 Client，如果 Model 需要使用不同的 Client，可以參考 <a href="https://github.com/elastic/elasticsearch-rails/tree/master/elasticsearch-model#the-elasticsearch-client" target="_blank" rel="noopener noreferrer">The Elasticsearch client</a> 的方式設定。</p>
<p>了解基礎設定後，我們便可透過 Model 進行搜尋了：</p>
<div class="language-ruby line-numbers-mode" data-highlighter="prismjs" data-ext="rb"><pre><code class="language-ruby"><span class="line">response <span class="token operator">=</span> Article<span class="token punctuation">.</span>search <span class="token string-literal"><span class="token string">'fox dogs'</span></span></span>
<span class="line"></span>
<span class="line">response<span class="token punctuation">.</span>results <span class="token comment"># Elasticsearch 搜尋的結果</span></span>
<span class="line">response<span class="token punctuation">.</span>records<span class="token punctuation">.</span>to_a <span class="token comment"># 把搜尋結果轉成 ActiveRecords</span></span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2>多 index 搜尋</h2>
<p><code>search</code> 方法本身支援多個 Model/Index 的搜尋，例如：</p>
<div class="language-ruby line-numbers-mode" data-highlighter="prismjs" data-ext="rb"><pre><code class="language-ruby"><span class="line">Elasticsearch<span class="token double-colon punctuation">::</span>Model<span class="token punctuation">.</span>search<span class="token punctuation">(</span><span class="token string-literal"><span class="token string">'fox'</span></span><span class="token punctuation">,</span> <span class="token punctuation">[</span>Article<span class="token punctuation">,</span> Comment<span class="token punctuation">]</span><span class="token punctuation">)</span><span class="token punctuation">.</span>results<span class="token punctuation">.</span>to_a<span class="token punctuation">.</span>map<span class="token punctuation">(</span><span class="token operator">&amp;</span><span class="token symbol">:to_hash</span><span class="token punctuation">)</span></span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div></div></div><p>不過這比較不適合我的使用場景，所以使用 <code>msearch</code> 方法，也就是 Elasticsearch 的 <a href="https://www.elastic.co/guide/en/elasticsearch/reference/current/search-multi-search.html" target="_blank" rel="noopener noreferrer">msearch</a> API，使用方式大同小異，只是把 payload 變 Ruby 的 Hash 而已，不過缺點是 <code>msearch</code> 沒有 <code>search</code> 那些像是 <code>records</code> 之類的方法可以用，只有單純的把 JSON 轉成 Hash 而已。</p>
<div class="language-ruby line-numbers-mode" data-highlighter="prismjs" data-ext="rb"><pre><code class="language-ruby"><span class="line">payload <span class="token operator">=</span> <span class="token punctuation">[</span></span>
<span class="line">  <span class="token punctuation">{</span> <span class="token symbol">index</span><span class="token operator">:</span> <span class="token string-literal"><span class="token string">'articles'</span></span> <span class="token punctuation">}</span><span class="token punctuation">,</span></span>
<span class="line">  <span class="token punctuation">{</span></span>
<span class="line">    <span class="token symbol">size</span><span class="token operator">:</span> <span class="token number">5</span><span class="token punctuation">,</span></span>
<span class="line">    <span class="token symbol">query</span><span class="token operator">:</span> <span class="token punctuation">{</span></span>
<span class="line">      <span class="token symbol">bool</span><span class="token operator">:</span> <span class="token punctuation">{</span></span>
<span class="line">        <span class="token symbol">must</span><span class="token operator">:</span> <span class="token punctuation">[</span></span>
<span class="line">          <span class="token punctuation">{</span></span>
<span class="line">            <span class="token symbol">multi_match</span><span class="token operator">:</span> <span class="token punctuation">{</span></span>
<span class="line">              <span class="token symbol">query</span><span class="token operator">:</span> <span class="token string-literal"><span class="token string">'Frontend Developers'</span></span><span class="token punctuation">,</span></span>
<span class="line">              <span class="token symbol">type</span><span class="token operator">:</span> <span class="token string-literal"><span class="token string">"most_fields"</span></span><span class="token punctuation">,</span></span>
<span class="line">              <span class="token symbol">operator</span><span class="token operator">:</span> <span class="token string-literal"><span class="token string">'and'</span></span><span class="token punctuation">,</span></span>
<span class="line">              <span class="token symbol">fields</span><span class="token operator">:</span> <span class="token punctuation">[</span><span class="token string-literal"><span class="token string">'title'</span></span><span class="token punctuation">,</span> <span class="token string-literal"><span class="token string">'description'</span></span><span class="token punctuation">]</span></span>
<span class="line">            <span class="token punctuation">}</span></span>
<span class="line">          <span class="token punctuation">}</span></span>
<span class="line">        <span class="token punctuation">]</span></span>
<span class="line">      <span class="token punctuation">}</span></span>
<span class="line">    <span class="token punctuation">}</span></span>
<span class="line">  <span class="token punctuation">}</span></span>
<span class="line"><span class="token punctuation">]</span></span>
<span class="line">Elasticsearch<span class="token double-colon punctuation">::</span>Model<span class="token punctuation">.</span>client<span class="token punctuation">.</span>msearch<span class="token punctuation">(</span>payload<span class="token punctuation">)</span></span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>如果要用 <code>msearch</code> 又想要有 <code>records</code> 的話，則必須要自己轉了，我們可以參考一下 <code>elastic-model</code> 在呼叫 <code>search</code> 後會做哪些事情，先來看一下 <a href="https://github.com/elastic/elasticsearch-rails/blob/80822d69a7/elasticsearch-model/lib/elasticsearch/model/searching.rb#L116" target="_blank" rel="noopener noreferrer">search</a> 的程式碼：</p>
<div class="language-ruby line-numbers-mode" data-highlighter="prismjs" data-ext="rb"><pre><code class="language-ruby"><span class="line"><span class="token keyword">class</span> <span class="token class-name">SearchRequest</span></span>
<span class="line">  <span class="token keyword">def</span> <span class="token method-definition"><span class="token function">execute</span></span><span class="token operator">!</span></span>
<span class="line">    klass<span class="token punctuation">.</span>client<span class="token punctuation">.</span>search<span class="token punctuation">(</span><span class="token variable">@definition</span><span class="token punctuation">)</span></span>
<span class="line">  <span class="token keyword">end</span></span>
<span class="line"><span class="token keyword">end</span></span>
<span class="line"></span>
<span class="line"><span class="token operator">...</span></span>
<span class="line"></span>
<span class="line"><span class="token keyword">module</span> <span class="token class-name">ClassMethods</span></span>
<span class="line">  <span class="token keyword">def</span> <span class="token method-definition"><span class="token function">search</span></span><span class="token punctuation">(</span>query_or_payload<span class="token punctuation">,</span> options<span class="token operator">=</span><span class="token punctuation">{</span><span class="token punctuation">}</span><span class="token punctuation">)</span></span>
<span class="line">    search   <span class="token operator">=</span> <span class="token class-name">SearchRequest</span><span class="token punctuation">.</span><span class="token keyword">new</span><span class="token punctuation">(</span><span class="token keyword">self</span><span class="token punctuation">,</span> query_or_payload<span class="token punctuation">,</span> options<span class="token punctuation">)</span></span>
<span class="line"></span>
<span class="line">    Response<span class="token double-colon punctuation">::</span><span class="token class-name">Response</span><span class="token punctuation">.</span><span class="token keyword">new</span><span class="token punctuation">(</span><span class="token keyword">self</span><span class="token punctuation">,</span> search<span class="token punctuation">)</span></span>
<span class="line">  <span class="token keyword">end</span></span>
<span class="line"><span class="token keyword">end</span></span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>我們可以知道會去建立一個 Response Object，所以來看一下 <a href="https://github.com/elastic/elasticsearch-rails/blob/80822d69a7f33a13fdfc294035bf57fa9777ff17/elasticsearch-model/lib/elasticsearch/model/response.rb#L29" target="_blank" rel="noopener noreferrer">Response Class</a> 的實作：</p>
<div class="language-ruby line-numbers-mode" data-highlighter="prismjs" data-ext="rb"><pre><code class="language-ruby"><span class="line"><span class="token keyword">def</span> <span class="token method-definition"><span class="token function">initialize</span></span><span class="token punctuation">(</span>klass<span class="token punctuation">,</span> search<span class="token punctuation">,</span> options<span class="token operator">=</span><span class="token punctuation">{</span><span class="token punctuation">}</span><span class="token punctuation">)</span></span>
<span class="line">  <span class="token variable">@klass</span>     <span class="token operator">=</span> klass</span>
<span class="line">  <span class="token variable">@search</span>    <span class="token operator">=</span> search</span>
<span class="line"><span class="token keyword">end</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># Returns the Elasticsearch response</span></span>
<span class="line"><span class="token comment">#</span></span>
<span class="line"><span class="token comment"># @return [Hash]</span></span>
<span class="line"><span class="token comment">#</span></span>
<span class="line"><span class="token keyword">def</span> <span class="token method-definition"><span class="token function">response</span></span></span>
<span class="line">  <span class="token variable">@response</span> <span class="token operator">||=</span> <span class="token class-name">HashWrapper</span><span class="token punctuation">.</span><span class="token keyword">new</span><span class="token punctuation">(</span>search<span class="token punctuation">.</span>execute<span class="token operator">!</span><span class="token punctuation">)</span></span>
<span class="line"><span class="token keyword">end</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># Returns the collection of records from the database</span></span>
<span class="line"><span class="token comment">#</span></span>
<span class="line"><span class="token comment"># @return [Records]</span></span>
<span class="line"><span class="token comment">#</span></span>
<span class="line"><span class="token keyword">def</span> <span class="token method-definition"><span class="token function">records</span></span><span class="token punctuation">(</span>options <span class="token operator">=</span> <span class="token punctuation">{</span><span class="token punctuation">}</span><span class="token punctuation">)</span></span>
<span class="line">  <span class="token variable">@records</span> <span class="token operator">||=</span> <span class="token class-name">Records</span><span class="token punctuation">.</span><span class="token keyword">new</span><span class="token punctuation">(</span>klass<span class="token punctuation">,</span> <span class="token keyword">self</span><span class="token punctuation">,</span> options<span class="token punctuation">)</span></span>
<span class="line"><span class="token keyword">end</span></span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>records</code> 方法就只是 new 一個  Records 物件，把 <code>klass</code>（也就是 Model，比方說你呼叫 <code>Article.search</code> 那就是 <code>Article</code> ）及 self （Response Object 傳進去﹚。</p>
<p>至此我就沒深追下去相關實作了，因為還有一些 Adapter 方面的實作蠻複雜的，不過可以確定一件事就是 <code>Records.new</code> 基本上可以得到跟　<code>Article.search('fox').records</code> 的行為一樣，所以我就做了一些嘗試，最終發現帶入對應的 Model Class、Hash Wrapper 即可。</p>
<p><code>Records.new(klass, self)</code> 中的 <code>klass</code> 就是 Response Class 的 <code>@klass</code>，這個 <code>@klass</code> 便是 new Response 時所帶入的 <code>self</code>，而這個 <code>self</code> 就是呼叫 <code>search</code> 方法的 Class，也就是 Model（<code>Article.search</code>、<code>User.search</code>）</p>
<div class="language-ruby line-numbers-mode" data-highlighter="prismjs" data-ext="rb"><pre><code class="language-ruby"><span class="line"><span class="token keyword">module</span> <span class="token class-name">ClassMethods</span></span>
<span class="line">  <span class="token keyword">def</span> <span class="token method-definition"><span class="token function">search</span></span><span class="token punctuation">(</span>query_or_payload<span class="token punctuation">,</span> options<span class="token operator">=</span><span class="token punctuation">{</span><span class="token punctuation">}</span><span class="token punctuation">)</span></span>
<span class="line">    search   <span class="token operator">=</span> <span class="token class-name">SearchRequest</span><span class="token punctuation">.</span><span class="token keyword">new</span><span class="token punctuation">(</span><span class="token keyword">self</span><span class="token punctuation">,</span> query_or_payload<span class="token punctuation">,</span> options<span class="token punctuation">)</span></span>
<span class="line"></span>
<span class="line">    Response<span class="token double-colon punctuation">::</span><span class="token class-name">Response</span><span class="token punctuation">.</span><span class="token keyword">new</span><span class="token punctuation">(</span><span class="token keyword">self</span><span class="token punctuation">,</span> search<span class="token punctuation">)</span></span>
<span class="line">  <span class="token keyword">end</span></span>
<span class="line"><span class="token keyword">end</span></span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>而 <code>Records.new(klass, self)</code> 中的 <code>self</code> 本應是 Response 物件，不過我嘗試 new 一個 Response 物件發現其會呼叫 <code>search.execute!</code> 或是 <code>@search</code> 的相關方法，因此無法先 new Response 物件，然後在 new Records，不過我們可以看到 <code>response</code> 方法是將其結果透過 <code>HashWrapper</code> 轉化出來的，於是我就嘗試著把 <code>msearch</code> 的結果，轉成 HashWrapper，然後丟給 Records，最終便可以得到把 Elasticsearch results 轉成 ActiveRecords。</p>
<p>相關實作程式碼：</p>
<div class="language-ruby line-numbers-mode" data-highlighter="prismjs" data-ext="rb"><pre><code class="language-ruby"><span class="line"><span class="token keyword">class</span> <span class="token class-name">Response</span></span>
<span class="line">  <span class="token keyword">def</span> <span class="token method-definition"><span class="token function">initialize</span></span><span class="token punctuation">(</span>response<span class="token punctuation">)</span></span>
<span class="line">    <span class="token variable">@response</span> <span class="token operator">=</span> response</span>
<span class="line">  <span class="token keyword">end</span></span>
<span class="line"></span>
<span class="line">  <span class="token keyword">def</span> <span class="token method-definition"><span class="token function">records</span></span></span>
<span class="line">    <span class="token variable">@records</span> <span class="token operator">||=</span> <span class="token variable">@response</span><span class="token punctuation">[</span><span class="token string-literal"><span class="token string">'responses'</span></span><span class="token punctuation">]</span><span class="token punctuation">.</span>reduce<span class="token punctuation">(</span><span class="token punctuation">{</span><span class="token punctuation">}</span><span class="token punctuation">)</span> <span class="token keyword">do</span> <span class="token operator">|</span>records<span class="token punctuation">,</span> r<span class="token operator">|</span></span>
<span class="line">      index_name <span class="token operator">=</span> get_index_of_first_doc<span class="token punctuation">(</span>r<span class="token punctuation">)</span> <span class="token comment"># 抓第一筆結果 index 是哪個，如：articles</span></span>
<span class="line"></span>
<span class="line">      <span class="token keyword">if</span> index_name<span class="token punctuation">.</span>present<span class="token operator">?</span></span>
<span class="line">        model <span class="token operator">=</span> index_name<span class="token punctuation">.</span>classify<span class="token punctuation">.</span>constantize <span class="token comment"># 把 index name 轉成 Model，如 Article</span></span>
<span class="line">        hash_wrapper <span class="token operator">=</span> Elasticsearch<span class="token double-colon punctuation">::</span>Model<span class="token double-colon punctuation">::</span><span class="token class-name">HashWrapper</span><span class="token punctuation">.</span><span class="token keyword">new</span><span class="token punctuation">(</span><span class="token punctuation">{</span><span class="token string-literal"><span class="token string">'response'</span></span><span class="token operator">:</span> r<span class="token punctuation">}</span><span class="token punctuation">)</span> <span class="token comment"># 把 results 轉成 HashWrapper</span></span>
<span class="line"></span>
<span class="line">        records_of_index <span class="token operator">=</span> Elasticsearch<span class="token double-colon punctuation">::</span>Model<span class="token double-colon punctuation">::</span>Response<span class="token double-colon punctuation">::</span><span class="token class-name">Records</span><span class="token punctuation">.</span><span class="token keyword">new</span><span class="token punctuation">(</span>model<span class="token punctuation">,</span> hash_wrapper<span class="token punctuation">)</span><span class="token punctuation">.</span>records<span class="token punctuation">.</span>to_a</span>
<span class="line">        <span class="token comment"># 把 articles index 的結果轉成 ActiveRecords 在這就像是</span></span>
<span class="line">        <span class="token comment"># Elasticsearch::Model::Response::Records.new(Article, hash_wrapper).records.to_a</span></span>
<span class="line">        records<span class="token punctuation">.</span>update<span class="token punctuation">(</span>index_name<span class="token punctuation">.</span>to_sym <span class="token operator">=></span> records_of_index<span class="token punctuation">)</span></span>
<span class="line">      <span class="token keyword">end</span></span>
<span class="line"></span>
<span class="line">      records</span>
<span class="line">    <span class="token keyword">end</span></span>
<span class="line">  <span class="token keyword">end</span></span>
<span class="line"></span>
<span class="line">  <span class="token keyword">private</span></span>
<span class="line">  <span class="token keyword">def</span> <span class="token method-definition"><span class="token function">get_index_of_first_doc</span></span><span class="token punctuation">(</span>result<span class="token punctuation">)</span></span>
<span class="line">    result<span class="token punctuation">[</span><span class="token string-literal"><span class="token string">'hits'</span></span><span class="token punctuation">]</span><span class="token punctuation">[</span><span class="token string-literal"><span class="token string">'hits'</span></span><span class="token punctuation">]</span><span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">]</span><span class="token punctuation">[</span><span class="token string-literal"><span class="token string">'_index'</span></span><span class="token punctuation">]</span> <span class="token keyword">if</span> result<span class="token punctuation">[</span><span class="token string-literal"><span class="token string">'hits'</span></span><span class="token punctuation">]</span><span class="token punctuation">[</span><span class="token string-literal"><span class="token string">'hits'</span></span><span class="token punctuation">]</span> <span class="token operator">&amp;&amp;</span> result<span class="token punctuation">[</span><span class="token string-literal"><span class="token string">'hits'</span></span><span class="token punctuation">]</span><span class="token punctuation">[</span><span class="token string-literal"><span class="token string">'hits'</span></span><span class="token punctuation">]</span><span class="token punctuation">.</span>length <span class="token operator">></span> <span class="token number">0</span></span>
<span class="line">  <span class="token keyword">end</span></span>
<span class="line"><span class="token keyword">end</span></span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>把回傳結果丟給 Response</p>
<div class="language-ruby line-numbers-mode" data-highlighter="prismjs" data-ext="rb"><pre><code class="language-ruby"><span class="line">payload <span class="token operator">=</span> <span class="token punctuation">{</span><span class="token punctuation">}</span> <span class="token comment"># your payload</span></span>
<span class="line">response <span class="token operator">=</span> Elasticsearch<span class="token double-colon punctuation">::</span>Model<span class="token punctuation">.</span>client<span class="token punctuation">.</span>msearch<span class="token punctuation">(</span><span class="token symbol">body</span><span class="token operator">:</span> payload<span class="token punctuation">)</span></span>
<span class="line"><span class="token class-name">Response</span><span class="token punctuation">.</span><span class="token keyword">new</span><span class="token punctuation">(</span>response<span class="token punctuation">)</span><span class="token punctuation">.</span>records</span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2>總結</h2>
<p>寫得有些凌亂，也不是很好懂，且 Records 後面我是靠猜測加實驗，並沒有深追實作方式及原理，最終發現可以如期得到我想要的結果，因此可能有部分理解錯誤、寫錯的地方，還請多多包涵、指正，總之 <code>mseach</code> 實作 <code>records</code> 的方法是做得到，只是大家的 <code>msearch</code> 搜尋方式及結果都不太一樣，可能無法像 <code>search</code> 的 response 這麼單純，所以可能需要修改成自己的使用場景。</p>
<p>另外這裡拿 document 的 <code>_index</code> 去轉成 Class，如果是使用 <code>alias</code> 的話，這裡的 <code>_index</code> 並不是 alias name，會導致找不到 Class 而噴錯</p>
<div class="language-ruby line-numbers-mode" data-highlighter="prismjs" data-ext="rb"><pre><code class="language-ruby"><span class="line">model <span class="token operator">=</span> index_name<span class="token punctuation">.</span>classify<span class="token punctuation">.</span>constantize <span class="token comment"># 把 index name 轉成 Model，如 Article</span></span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div></div></div><p>所以其實不是那麼推薦使用這種方法，尤其有 Alias 的 index</p>
]]></content:encoded>
    </item>
    <item>
      <title>Pixel 3 XL 送 fun2repair 維修經驗</title>
      <link>https://blog.giiin.dev/posts/Miscellaneous/2021/pixel-3-xl-fun2repair-experience.html</link>
      <guid>https://blog.giiin.dev/posts/Miscellaneous/2021/pixel-3-xl-fun2repair-experience.html</guid>
      <source url="https://blog.giiin.dev/rss.xml">Pixel 3 XL 送 fun2repair 維修經驗</source>
      <description>前言
前陣子跑去租重機來騎，店裡提供手機架的服務，但 Pixel 3 XL 加上 Google Case 實在太肥，手機架放不下，索性把手機殼拔掉，結果好死不死就發生悲劇，手機從車上跳車，好險是背面著地，手機背板就破裂了，外加鏡頭表面破裂（鏡頭沒傷到，外面有一層玻璃之類的破裂），只好送去 fun2repair 維修。
建議流程
1. 備份資料、照片
有...</description>
      <category>Miscullaneous</category>
      <pubDate>Thu, 08 Apr 2021 00:00:00 GMT</pubDate>
      <content:encoded><![CDATA[<h2>前言</h2>
<p>前陣子跑去租重機來騎，店裡提供手機架的服務，但 Pixel 3 XL 加上 Google Case 實在太肥，手機架放不下，索性把手機殼拔掉，結果好死不死就發生悲劇，手機從車上跳車，好險是背面著地，手機背板就破裂了，外加鏡頭表面破裂（鏡頭沒傷到，外面有一層玻璃之類的破裂），只好送去 fun2repair 維修。</p>
<h2>建議流程</h2>
<p><strong>1. 備份資料、照片</strong></p>
<p>有用 Google Authenticator 記得轉移設備，App 就隨緣，但我後來手機拿回來 Google Play 有紀錄，可以重裝回來</p>
<p><strong>2. 格式化手機</strong></p>
<p>你不格式化，fun2repair 應該也會格式化，但為了自己資料安全建議格式化</p>
<p><strong>3. 拔出 SIM 卡</strong></p>
<p>建議準備備用機（物流去收件、送件給你可能會聯絡）</p>
<p><strong>4. 拆掉配件（手機殼等）</strong></p>
<p><strong>5. 準備好盒子、氣泡紙等緩衝，並包裝好</strong></p>
<p>當時我不知道要自己包裝，以為物流會有盒子之類，結果物流跟我說他們只會幫我裸機放進袋子，風險很大</p>
<p><strong>6. 填寫官網維修申請</strong></p>
<p>建議備份好、格式化、包裝好、有備用機可以連絡後，在填寫申請維修，因為我當時人在台北市中山區，我送申請五分鐘後，物流就來取件了，如果你沒先弄好這些，你會很趕並讓物流人員等你。</p>
<h2>時間軸</h2>
<ul>
<li>2021-03-30
<ul>
<li>11:01 提出維修申請</li>
<li>11:08 順豐取件</li>
</ul>
</li>
<li>2021-03-31
<ul>
<li>09:33 fun2repair 收件</li>
<li>13:30 fun2repair 檢測完、報價</li>
<li>14:00 匯款（結果還是要等隔天才會開始修，提早匯不見得會當天修）</li>
</ul>
</li>
<li>2021-04-01
<ul>
<li>16:10 順豐至 fun2repair 取件</li>
</ul>
</li>
<li>2021-04-02 (開始清明連假)
<ul>
<li>06:10 快件到達 【台北中山新生北二段理貨中心】</li>
</ul>
</li>
<li>2021-04-06 收件（連假我不在台北，不然其實 4/2 就拿的到手機了）</li>
</ul>
<p>總結其實從送修到拿到手機至少要 4 天，不確定能否更短，但如果匯款完隔天開始修，且當天修完寄出至少 4 天，不過 fun2repair 維修天數通常會保守估計抓 3~5 工作天，以上僅供參考。</p>
<h2>結論</h2>
<p>這次好險只摔到背板、鏡頭表面，fun2repair 的報價是含電池老化更換，一共 $2,400 元，如果不修一樣要支付 460 元的檢測費 &amp; 物流費，除了網站做的不是這麼好之外，其他我是覺得普普通通，不過不能寄 RMA 回去 Google 換整新機有點難過。</p>
]]></content:encoded>
    </item>
    <item>
      <title>Git Push 到 Private EC2 Instance</title>
      <link>https://blog.giiin.dev/posts/AWS/2021/git-push-to-private-ec2.html</link>
      <guid>https://blog.giiin.dev/posts/AWS/2021/git-push-to-private-ec2.html</guid>
      <source url="https://blog.giiin.dev/rss.xml">Git Push 到 Private EC2 Instance</source>
      <description>前言
最近開始使用 Beancount 作帳，除了有日記帳，還能產資產負債表、損益表，不過是使用文字記帳，沒有資料庫等服務，而好處是可以做版本控制，所以希望在自己 Server 上，建立一個 Git Remote Repository 給我的其他設備可以同步 Beancount 帳本資料。
因為帳務資料較為私密的資料，不希望存放在 GitHub 之類，...</description>
      <category>AWS</category>
      <pubDate>Mon, 22 Feb 2021 00:00:00 GMT</pubDate>
      <content:encoded><![CDATA[<h2>前言</h2>
<p>最近開始使用 <a href="https://github.com/beancount/beancount" target="_blank" rel="noopener noreferrer">Beancount</a> 作帳，除了有日記帳，還能產資產負債表、損益表，不過是使用文字記帳，沒有資料庫等服務，而好處是可以做版本控制，所以希望在自己 Server 上，建立一個 Git Remote Repository 給我的其他設備可以同步 Beancount 帳本資料。</p>
<p>因為帳務資料較為私密的資料，不希望存放在 GitHub 之類，決定自己架台 Git Server，所以開一台在 Private Subnet 的 EC2 當 Git Server。</p>
<h2>VPC 架構</h2>
<p>建立一個 VPC 包含：</p>
<ul>
<li>1 個 Public Subnet</li>
<li>1 個 Private Subnet</li>
</ul>
<p>架構上有點像是下面這張圖，只是是單 AZ（Availability Zone）：
<img src="https://d1.awsstatic.com/partner-network/QuickStart/datasheets/linux-bastion-architecture.584765ff724625db9ab0d91a8ccb1c2eb7e15a5b.png" alt="AWS - Linux Bastion Architecture"></p>
<p>所以大致上就是像上圖的左半部一樣，Public Subnet 指到一個 Internet Gateway，然後起一台 EC2 當 Linux Bastion 放在 Public Subnet，然後開 22 port 給自己連（沒設定任何 Auto Scaling、EIP），甚至這台 EC2 我都用 Spot Instance，反正一天連一次，instance 沒了再開就好，但 spot 的價格讓我比較負擔的起。</p>
<p>接著再起一台 EC2 當存放 Git Remote Repository 的機器，放在 Private Subnet 並允許此 VPC 內的機器可以透過 SSH 連線進來，至少 Bastion 那台要能連線進來。</p>
<h2>建立 Git Remote Repository</h2>
<p>當兩台機器 (Bastion、Git Server) 開好之後，可以先透過 SSH 連線到 Bastion 再連線到 Git Server 看連線是否正常（可用 SSH Agent 等方式節省連線繁瑣的程序），如果無法連線，可能 VPC 的設定有錯誤。</p>
<p>接著我們進到 Git Server 進行 Git 設定，可以參考 Pro Git <a href="https://git-scm.com/book/en/v2/Git-on-the-Server-Setting-Up-the-Server" target="_blank" rel="noopener noreferrer">4.4 Git on the Server - Setting Up the Server</a> 的步驟做。</p>
<p><strong>建立 git user</strong></p>
<div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code class="language-bash"><span class="line"><span class="token comment"># Git Server</span></span>
<span class="line"><span class="token function">sudo</span> adduser <span class="token function">git</span></span>
<span class="line"><span class="token function">su</span> <span class="token function">git</span></span>
<span class="line"><span class="token builtin class-name">cd</span></span>
<span class="line"><span class="token function">mkdir</span> .ssh <span class="token operator">&amp;&amp;</span> <span class="token function">chmod</span> <span class="token number">700</span> .ssh</span>
<span class="line"><span class="token function">touch</span> .ssh/authorized_keys <span class="token operator">&amp;&amp;</span> <span class="token function">chmod</span> <span class="token number">600</span> .ssh/authorized_keys</span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>把你的 Public Key 放入 authorized_keys</strong></p>
<div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code class="language-bash"><span class="line"><span class="token comment"># Git Server</span></span>
<span class="line"><span class="token function">cat</span> /tmp/id_rsa.john.pub <span class="token operator">>></span> ~/.ssh/authorized_keys</span>
<span class="line"><span class="token function">cat</span> /tmp/id_rsa.josie.pub <span class="token operator">>></span> ~/.ssh/authorized_keys</span>
<span class="line"><span class="token function">cat</span> /tmp/id_rsa.jessica.pub <span class="token operator">>></span> ~/.ssh/authorized_keys</span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>建立 remote repository</strong></p>
<div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code class="language-bash"><span class="line"><span class="token comment"># Git Server</span></span>
<span class="line"><span class="token builtin class-name">cd</span> /home/git</span>
<span class="line"><span class="token function">mkdir</span> project.git</span>
<span class="line"><span class="token builtin class-name">cd</span> project.git</span>
<span class="line"><span class="token function">git</span> init <span class="token parameter variable">--bare</span></span>
<span class="line">Initialized empty Git repository <span class="token keyword">in</span> /srv/git/project.git/</span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>截至目前為止 remote repository 已經建好了，上述步驟相當於你在 GitHub 建立一個 Repository。</p>
<h2>設定 SSH 連線</h2>
<p>透過編輯 <code>.ssh/config</code> 的方式我們可以簡單的直接連線到 Git Server，不用自己先連到 Bastion 再連到 Git Server。</p>
<div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code class="language-bash"><span class="line"><span class="token comment"># 你的電腦</span></span>
<span class="line"><span class="token function">vim</span> ~/.ssh/config</span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div></div></div><p>假設我們兩台機器的 IP:</p>
<ul>
<li>Bastion: <code>35.174.52.219</code> (Public IPv4)</li>
<li>Git Server: <code>192.168.0.34</code> (Private IPv4)</li>
</ul>
<p>我們的 <code>.ssh/config</code> 如下：</p>
<div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">Host 35.174.52.219</span>
<span class="line">    HostName 35.174.52.219</span>
<span class="line">    User ec2-user</span>
<span class="line">    IdentityFile /your/ec2/key_path.pem</span>
<span class="line"></span>
<span class="line">Host 192.168.0.34</span>
<span class="line">    HostName 192.168.0.34</span>
<span class="line">    User ec2-user</span>
<span class="line">    IdentityFile /your/ec2/key_path.pem</span>
<span class="line">    ProxyCommand ssh -W %h:%p 35.174.52.219</span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>存檔後，便可直接透過 <code>ssh 192.168.0.34</code> 連線到 Git Server，後面設定 Git Remote 後，也才能透過 Git Push 將我們 local 的 repository 同步到 remote repository。</p>
<h2>設定 Project 的 remote</h2>
<p>接著我們回到 local 的 repository</p>
<div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code class="language-bash"><span class="line"><span class="token builtin class-name">cd</span> ~/development/your_project</span>
<span class="line"><span class="token function">git</span> remote <span class="token function">add</span> origin git@192.168.0.34:/home/git/project.git</span>
<span class="line"><span class="token function">git</span> push origin master</span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>應該就能成功了：</p>
<div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">Counting objects: 4, done.</span>
<span class="line">Delta compression using up to 12 threads.</span>
<span class="line">Compressing objects: 100% (2/2), done.</span>
<span class="line">Writing objects: 100% (4/4), 528 bytes | 528.00 KiB/s, done.</span>
<span class="line">Total 4 (delta 1), reused 0 (delta 0)</span>
<span class="line">To 192.168.0.34:/home/git/beancount.git</span>
<span class="line">   0365711..5ecceb5  master -> master</span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2>參考資料</h2>
<ul>
<li><a href="https://git-scm.com/book/en/v2/Git-on-the-Server-Setting-Up-the-Server" target="_blank" rel="noopener noreferrer">Pro Git</a></li>
<li><a href="https://stackoverflow.com/questions/32654857/git-clone-issues-via-an-ssh-proxied-host?fbclid=IwAR3SvXOobzGk14H8nc3Mxc_Er5Tiz6wC6izz2AtNiakBpV2DpHCs97l5idk" target="_blank" rel="noopener noreferrer">git clone issues via an SSH proxied host</a></li>
<li><a href="https://www.cyberciti.biz/faq/linux-unix-ssh-proxycommand-passing-through-one-host-gateway-server/" target="_blank" rel="noopener noreferrer">SSH ProxyCommand example: Going through one host to reach another server</a></li>
</ul>
]]></content:encoded>
      <enclosure url="https://d1.awsstatic.com/partner-network/QuickStart/datasheets/linux-bastion-architecture.584765ff724625db9ab0d91a8ccb1c2eb7e15a5b.png" type="image/png"/>
    </item>
    <item>
      <title>Ubuntu 上 MySQL 5.7.32 的那些 my.cnf</title>
      <link>https://blog.giiin.dev/posts/MySQL/2021/my_cnf_difference_between_conf_and_mysql_conf.html</link>
      <guid>https://blog.giiin.dev/posts/MySQL/2021/my_cnf_difference_between_conf_and_mysql_conf.html</guid>
      <source url="https://blog.giiin.dev/rss.xml">Ubuntu 上 MySQL 5.7.32 的那些 my.cnf</source>
      <description>前言
最近因為使用 MySQL 的 JSON Aggregate 的一些 function，發現原系統是 5.7.21，但 JSON Aggregate Functions 在 5.7.22 才支援，加上系統有點舊，無法順利更新上 5.7.32，想說直接開一台較新的作業系統 (Ubuntu 18.04)，安裝新版 5.7.32，在進行一些設定時，發現 ...</description>
      <category>MySQL</category>
      <pubDate>Thu, 07 Jan 2021 00:00:00 GMT</pubDate>
      <content:encoded><![CDATA[<h2>前言</h2>
<p>最近因為使用 MySQL 的 JSON Aggregate 的一些 function，發現原系統是 <code>5.7.21</code>，但 JSON Aggregate Functions 在 <code>5.7.22</code> 才支援，加上系統有點舊，無法順利更新上 <code>5.7.32</code>，想說直接開一台較新的作業系統 (Ubuntu 18.04)，安裝新版 <code>5.7.32</code>，在進行一些設定時，發現 <code>5.7</code> 版的 MySQL 設定好繚亂，不是很清楚要設定哪個 configuration file。</p>
<h2>版本差異</h2>
<p>在 <code>/etc/mysql</code> 底下有下列這些檔案：</p>
<div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">- my.cnf</span>
<span class="line">- my.cnf.fallback </span>
<span class="line">- mysql.cnf</span>
<span class="line">> conf.d</span>
<span class="line"> - mysql.cnf</span>
<span class="line"> - mysqldump.cnf</span>
<span class="line">> mysql.conf.d</span>
<span class="line"> - mysqld.cnf</span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>首先有 <code>my.cnf</code> 及 <code>mysql.cnf</code> 這兩個檔案，究竟有何差異？
透過指令我們可以發現 <code>my.cnf</code> 其實是 link 到 <code>/etc/alternatives/my.cnf</code>。</p>
<div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">my.cnf -> /etc/alternatives/my.cnf</span>
<span class="line">mysql.cnf</span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div></div></div><p>再查看一下 <code>/etc/alternatives/my.cnf</code>，可以發現是 link 回 <code>/etc/mysql/mysql.cnf</code>。</p>
<div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">/etc/alternatives/my.cnf -> /etc/mysql/mysql.cnf</span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div></div></div><p>所以無論你改 <code>/etc/mysql/mysql.cnf</code> 或 <code>/etc/mysql/my.cnf</code> 其實他們都是長一樣的。接著我們再來看 <code>/etc/mysql/mysql.cnf</code> 裡面的內容：</p>
<div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">!includedir /etc/mysql/conf.d/</span>
<span class="line">!includedir /etc/mysql/mysql.conf.d/</span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div></div></div><p>會把所有 <code>/etc/mysql/conf.d/</code> 及 <code>/etc/mysql/mysql.conf.d/</code> 裡面的 <code>.cnf</code> 檔案匯入，但 <code>conf.d/</code> 跟 <code>mysql.conf.d</code> 的差異是什麼呢？查到一個<a href="https://serverfault.com/a/954306" target="_blank" rel="noopener noreferrer">討論</a>是說：</p>
<blockquote>
<p>!includedir /etc/mysql/mysql.conf.d/ is an SYSCONFDIR generate by ubuntu when Mysql was compiling for the package in CMAKE option.</p>
</blockquote>
<p>以及這個答案的第一個 comment：</p>
<blockquote>
<p>Thanks for your reply. So I understand that /etc/mysql/mysql.conf.d is there just for retrocompatibility and /etc/mysql/conf.d is the way to go, right?</p>
</blockquote>
<p>在經過我自己的一些測試後，發現 <code>mysql.conf.d</code> 資料夾是在 <code>5.7</code> 版本才出現的，我起兩台全新的 Ubuntu 14.04 Server，分別安裝 MySQL <code>5.5</code> 跟 <code>5.6</code>，<code>/etc/mysql</code> 下只有 <code>conf.d</code> 資料夾，並沒有 <code>mysql.conf.d</code>，再起一台 Ubuntu 18.04 Server 安裝 MySQL <code>5.7</code> 才出現 <code>mysql.conf.d</code>。</p>
<p>另外在 <a href="https://dev.mysql.com/doc/refman/5.7/en/using-systemd.html" target="_blank" rel="noopener noreferrer">MySQL 5.7 Reference Manual - 2.5.10 Managing MySQL Server with systemd</a> 文件有提到</p>
<blockquote>
<p>To use multiple-instance capability, modify the my.cnf option file to include configuration of key options for each instance. These file locations are typical:</p>
<ul>
<li>/etc/my.cnf or /etc/mysql/my.cnf (RPM platforms)</li>
<li>/etc/mysql/mysql.conf.d/mysqld.cnf (Debian platforms)</li>
</ul>
</blockquote>
<p>官方文件寫到可以使用 <code>multiple-instance capability</code> 可以修改 my.cnf option file，雖然不是在 option file 的章節裡面寫，而是在一個奇怪的地方寫 option file 一般存放的地方，但從這裡我們可以發現，官方有說一般這個檔案在 RPM 跟 Debian 系統的路徑在哪。</p>
<h2>結論</h2>
<p>我們可以發現 <code>5.7</code> 版本會出現 <code>mysql.conf.d</code> 資料夾，加上官方文件有提及 Debian 平台可以修改 <code>/etc/mysql/mysql.conf.d/mysqld.cnf</code>，所以我個人理解為 Debian 平台 <code>/etc/mysql/mysql.conf.d/mysqld.cnf</code> 是官方建議修改的地方，而 <code>conf.d</code> 是為兼容舊版使用。</p>
<p>當然在新系統安裝完 MySQL <code>5.7</code> 後，可以發現只有 <code>/etc/mysql/mysql.conf.d/mysqld.cnf</code> 裡面有詳細的設定，而 <code>/etc/mysql/conf.d/my.cnf</code> 是空的，但對於 MySQL 從 <code>5.6</code> 升級上來的，可能就有彼此改在不同檔案的問題，最好的方式還是團隊溝通、建立共識改單一一個 configuration file，避免造成設定被覆蓋，是比較重要的。</p>
<h2>參考資料</h2>
<ul>
<li><a href="https://dev.mysql.com/doc/refman/5.7/en/option-files.html" target="_blank" rel="noopener noreferrer">MySQL 5.7 Reference Manual - 4.2.2.2 Using Option Files</a></li>
<li><a href="https://dev.mysql.com/doc/refman/5.7/en/using-systemd.html" target="_blank" rel="noopener noreferrer">MySQL 5.7 Reference Manual - 2.5.10 Managing MySQL Server with systemd</a></li>
<li><a href="https://serverfault.com/a/954306" target="_blank" rel="noopener noreferrer">MySQL 8 on Ubuntu: what's the difference between /etc/mysql/conf.d/ and /etc/mysql/mysql.conf.d/?</a></li>
</ul>
]]></content:encoded>
    </item>
    <item>
      <title>如何把 S3 上的檔案透過 ActiveStorage attach 回 Rails 的 record</title>
      <link>https://blog.giiin.dev/posts/Ruby%20on%20Rails/2020/attach_s3_file_with_activestorage_1213.html</link>
      <guid>https://blog.giiin.dev/posts/Ruby%20on%20Rails/2020/attach_s3_file_with_activestorage_1213.html</guid>
      <source url="https://blog.giiin.dev/rss.xml">如何把 S3 上的檔案透過 ActiveStorage attach 回 Rails 的 record</source>
      <description>前言
最近開發一個功能是將 ActiveStorage 上傳至 S3 的檔案，進行轉檔後，在將轉完的檔案 attach 回該 record，假設有一個 model 叫做 User，並且有兩個 ActiveStorage has_one_attached 的欄位，一個是 original_file，另一個 converted_file，我可以透過 Use...</description>
      <category>Ruby on Rails</category>
      <pubDate>Sun, 13 Dec 2020 00:00:00 GMT</pubDate>
      <content:encoded><![CDATA[<h2>前言</h2>
<p>最近開發一個功能是將 ActiveStorage 上傳至 S3 的檔案，進行轉檔後，在將轉完的檔案 attach 回該 record，假設有一個 model 叫做 <code>User</code>，並且有兩個 ActiveStorage <code>has_one_attached</code> 的欄位，一個是 <code>original_file</code>，另一個 <code>converted_file</code>，我可以透過 <code>User.original_file</code> 及 <code>User.converted_file</code> 抓到原始及轉檔後的檔案。</p>
<p>透過 ActiveStorage 上傳檔案到 GCP、S3 很簡單，官方文件寫的很清楚，但如果你的檔案是透過其他服務（轉檔服務）等放到 S3，而不是透過你本身的 Application 上傳，要如何把已經在 S3 的檔案關聯回 record 呢？下面透過 ActiveStorage source code 一步一步說明如何達到此需求。</p>
<h2>ActiveStorage 原理</h2>
<p>在 <a href="https://edgeguides.rubyonrails.org/active_storage_overview.html" target="_blank" rel="noopener noreferrer">ActiveStorage 文件</a>中，並沒有提到如何把已經在 S3 的檔案，在綁回 model 中 ActiveStorage 的欄位，雖然在 StackOverflow 有找到解答，但並不清楚 ActiveStorage 的運作方式，所以也不明白為何要這麼寫，因此了解 ActiveStorage 的原理才能知道為何要這麼寫！</p>
<p>以下將以 Rails v5.2.0 版來解析 ActiveStorage 原理，Storage Service 也都會以 S3 為主（當然其他 Service 也大同小異），其程式碼可以參考此<a href="https://github.com/rails/rails/tree/v5.2.0/activestorage" target="_blank" rel="noopener noreferrer">連結</a>。</p>
<h3>資料表結構</h3>
<p>從 <a href="https://github.com/rails/rails/blob/v5.2.0/activestorage/db/migrate/20170806125915_create_active_storage_tables.rb" target="_blank" rel="noopener noreferrer">migrations</a> 可以發現 ActiveStorage 新增 2 張資料表</p>
<div class="language-Ruby line-numbers-mode" data-highlighter="prismjs" data-ext="Ruby"><pre><code class="language-Ruby"><span class="line">create_table :active_storage_blobs do |t|</span>
<span class="line">  t.string   :key,        null: false</span>
<span class="line">  t.string   :filename,   null: false</span>
<span class="line">  t.string   :content_type</span>
<span class="line">  t.text     :metadata</span>
<span class="line">  t.bigint   :byte_size,  null: false</span>
<span class="line">  t.string   :checksum,   null: false</span>
<span class="line">  t.datetime :created_at, null: false</span>
<span class="line"></span>
<span class="line">  t.index [ :key ], unique: true</span>
<span class="line">end</span>
<span class="line"></span>
<span class="line">create_table :active_storage_attachments do |t|</span>
<span class="line">  t.string     :name,     null: false</span>
<span class="line">  t.references :record,   null: false, polymorphic: true, index: false</span>
<span class="line">  t.references :blob,     null: false</span>
<span class="line"></span>
<span class="line">  t.datetime :created_at, null: false</span>
<span class="line"></span>
<span class="line">  t.index [ :record_type, :record_id, :name, :blob_id ], name: &quot;index_active_storage_attachments_uniqueness&quot;, unique: true</span>
<span class="line">end</span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ul>
<li><code>active_storage_blobs</code>: 儲存檔案相關內容
<ul>
<li><code>key</code>: 存在 Service 上的名稱，如：S3 上檔案的 key</li>
<li><code>filename</code>: 原始（上傳時）的檔案名稱</li>
<li><code>content_type</code>: 檔案的 Content Type (Media Type)</li>
<li><code>metadata</code>: 一些 metadata</li>
<li><code>byte_size</code>: 檔案大小</li>
<li><code>checksum</code>: 用來檢查檔案送到 Service 後，與當初計算的是正確（同個檔案）</li>
</ul>
</li>
<li><code>active_storage_attachments</code>: 儲存關聯的 Model 及 Blob
<ul>
<li><code>name</code>: Model 的 ActiveStorage 欄位名稱，如：<code>original_file</code></li>
<li><code>record_type</code>: Model 名稱，如：<code>User</code></li>
<li><code>record_id</code>: 關聯 Model 的 <code>id</code></li>
<li><code>blob_id</code>: 關聯 Blob 的 <code>id</code></li>
</ul>
</li>
</ul>
<p>這裡比較難懂的應該是 <code>checksum</code>，自己當初有點好奇為何需要 <code>checksum</code>（抱歉，我菜QQ）。
我們來看一下 ActiveStorage upload 做了什麼事情</p>
<p><a href="https://github.com/rails/rails/blob/v5.2.0/activestorage/app/models/active_storage/blob.rb#L139" target="_blank" rel="noopener noreferrer">activestorage/app/models/active_storage/blob.rb</a></p>
<div class="language-ruby line-numbers-mode" data-highlighter="prismjs" data-ext="rb"><pre><code class="language-ruby"><span class="line">  <span class="token comment"># Prior to uploading, we compute the checksum, which is sent to the service for transit integrity validation. If the</span></span>
<span class="line">  <span class="token comment"># checksum does not match what the service receives, an exception will be raised. We also measure the size of the +io+</span></span>
<span class="line">  <span class="token comment"># and store that in +byte_size+ on the blob record.</span></span>
<span class="line">  <span class="token comment">#</span></span>
<span class="line">  <span class="token comment"># Normally, you do not have to call this method directly at all. Use the factory class methods of +build_after_upload+</span></span>
<span class="line">  <span class="token comment"># and +create_after_upload!+.</span></span>
<span class="line">  <span class="token keyword">def</span> <span class="token method-definition"><span class="token function">upload</span></span><span class="token punctuation">(</span>io<span class="token punctuation">)</span></span>
<span class="line">    <span class="token keyword">self</span><span class="token punctuation">.</span>checksum     <span class="token operator">=</span> compute_checksum_in_chunks<span class="token punctuation">(</span>io<span class="token punctuation">)</span></span>
<span class="line">    <span class="token keyword">self</span><span class="token punctuation">.</span>content_type <span class="token operator">=</span> extract_content_type<span class="token punctuation">(</span>io<span class="token punctuation">)</span></span>
<span class="line">    <span class="token keyword">self</span><span class="token punctuation">.</span>byte_size    <span class="token operator">=</span> io<span class="token punctuation">.</span>size</span>
<span class="line">    <span class="token keyword">self</span><span class="token punctuation">.</span>identified   <span class="token operator">=</span> <span class="token boolean">true</span></span>
<span class="line"></span>
<span class="line">    service<span class="token punctuation">.</span>upload<span class="token punctuation">(</span>key<span class="token punctuation">,</span> io<span class="token punctuation">,</span> <span class="token symbol">checksum</span><span class="token operator">:</span> checksum<span class="token punctuation">)</span></span>
<span class="line">  <span class="token keyword">end</span></span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><blockquote>
<p>Prior to uploading, we compute the checksum, which is sent to the service for transit integrity validation.</p>
</blockquote>
<p>仔細看第一行註解，這裡有敘述 upload 實作的詳細過程，而也提到 <code>checksum</code> 扮演的角色。
接著我們來看 S3 這個 Service upload 是如何實作的吧</p>
<p><a href="https://github.com/rails/rails/blob/v5.2.0/activestorage/lib/active_storage/service/s3_service.rb" target="_blank" rel="noopener noreferrer">active_storage/service/s3_service.rb</a></p>
<div class="language-ruby line-numbers-mode" data-highlighter="prismjs" data-ext="rb"><pre><code class="language-ruby"><span class="line"><span class="token keyword">def</span> <span class="token method-definition"><span class="token function">upload</span></span><span class="token punctuation">(</span>key<span class="token punctuation">,</span> io<span class="token punctuation">,</span> <span class="token symbol">checksum</span><span class="token operator">:</span> <span class="token keyword">nil</span><span class="token punctuation">)</span></span>
<span class="line">  instrument <span class="token symbol">:upload</span><span class="token punctuation">,</span> <span class="token symbol">key</span><span class="token operator">:</span> key<span class="token punctuation">,</span> <span class="token symbol">checksum</span><span class="token operator">:</span> checksum <span class="token keyword">do</span></span>
<span class="line">    <span class="token keyword">begin</span></span>
<span class="line">      object_for<span class="token punctuation">(</span>key<span class="token punctuation">)</span><span class="token punctuation">.</span>put<span class="token punctuation">(</span>upload_options<span class="token punctuation">.</span>merge<span class="token punctuation">(</span><span class="token symbol">body</span><span class="token operator">:</span> io<span class="token punctuation">,</span> <span class="token symbol">content_md5</span><span class="token operator">:</span> checksum<span class="token punctuation">)</span><span class="token punctuation">)</span></span>
<span class="line">    <span class="token keyword">rescue</span> Aws<span class="token double-colon punctuation">::</span><span class="token constant">S3</span><span class="token double-colon punctuation">::</span>Errors<span class="token double-colon punctuation">::</span>BadDigest</span>
<span class="line">      <span class="token keyword">raise</span> ActiveStorage<span class="token double-colon punctuation">::</span>IntegrityError</span>
<span class="line">    <span class="token keyword">end</span></span>
<span class="line">  <span class="token keyword">end</span></span>
<span class="line"><span class="token keyword">end</span></span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>簡單來說就是上傳前我們會對檔案做計算，會產生一組 MD5 的 hash，上傳時會把 <code>checksum</code> 也給 S3，S3 收到檔案後一樣會做計算，如果與 <code>checksum</code> 不同，則會 raise <code>Aws::S3::Errors::BadDigest</code>，以此去避免檔案內容損壞、竄改。</p>
<p>可參考：</p>
<ul>
<li><a href="https://docs.aws.amazon.com/sdk-for-ruby/v2/api/Aws/S3/Object.html#put-instance_method" target="_blank" rel="noopener noreferrer">S3 v2 SDK for ruby - #put</a></li>
<li><a href="https://aws.amazon.com/tw/premiumsupport/knowledge-center/data-integrity-s3/" target="_blank" rel="noopener noreferrer">How can I check the integrity of an object uploaded to Amazon S3?</a></li>
</ul>
<h3>建立檔案已經在 S3 上的 Blob</h3>
<p>雖然 ActiveStorage 的 Tutorial 內沒寫，不過在 API Docs 跟 ActiveStorage Source Code 有說明如何建立一個已經上傳的 Blob。</p>
<p><a href="https://github.com/rails/rails/blob/v5.2.0/activestorage/app/models/active_storage/blob.rb#L3" target="_blank" rel="noopener noreferrer">activestorage/app/models/active_storage/blob.rb#L3</a></p>
<div class="language-ruby line-numbers-mode" data-highlighter="prismjs" data-ext="rb"><pre><code class="language-ruby"><span class="line"><span class="token comment"># A blob is a record that contains the metadata about a file and a key for where that file resides on the service.</span></span>
<span class="line"><span class="token comment"># Blobs can be created in two ways:</span></span>
<span class="line"><span class="token comment">#</span></span>
<span class="line"><span class="token comment"># 1. Subsequent to the file being uploaded server-side to the service via &lt;tt>create_after_upload!&lt;/tt>.</span></span>
<span class="line"><span class="token comment"># 2. Ahead of the file being directly uploaded client-side to the service via &lt;tt>create_before_direct_upload!&lt;/tt>.</span></span>
<span class="line"><span class="token comment">#</span></span>
<span class="line"><span class="token comment"># The first option doesn't require any client-side JavaScript integration, and can be used by any other back-end</span></span>
<span class="line"><span class="token comment"># service that deals with files. The second option is faster, since you're not using your own server as a staging</span></span>
<span class="line"><span class="token comment"># point for uploads, and can work with deployments like Heroku that do not provide large amounts of disk space.</span></span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>根據註解說明：如果檔案已經被上傳到 Service，則可以透過 <code>create_before_direct_upload!</code> 建立 Blob，那麼我們就來直接看 <code>create_before_direct_upload!</code> 的實作。</p>
<p><a href="https://github.com/rails/rails/blob/v5.2.0/activestorage/app/models/active_storage/blob.rb#L64" target="_blank" rel="noopener noreferrer">activestorage/app/models/active_storage/blob.rb#L64</a></p>
<div class="language-ruby line-numbers-mode" data-highlighter="prismjs" data-ext="rb"><pre><code class="language-ruby"><span class="line"><span class="token comment"># Returns a saved blob _without_ uploading a file to the service. This blob will point to a key where there is</span></span>
<span class="line"><span class="token comment"># no file yet. It's intended to be used together with a client-side upload, which will first create the blob</span></span>
<span class="line"><span class="token comment"># in order to produce the signed URL for uploading. This signed URL points to the key generated by the blob.</span></span>
<span class="line"><span class="token comment"># Once the form using the direct upload is submitted, the blob can be associated with the right record using</span></span>
<span class="line"><span class="token comment"># the signed ID.</span></span>
<span class="line"><span class="token keyword">def</span> <span class="token method-definition"><span class="token function">create_before_direct_upload</span></span><span class="token operator">!</span><span class="token punctuation">(</span><span class="token symbol">filename</span><span class="token operator">:</span><span class="token punctuation">,</span> <span class="token symbol">byte_size</span><span class="token operator">:</span><span class="token punctuation">,</span> <span class="token symbol">checksum</span><span class="token operator">:</span><span class="token punctuation">,</span> <span class="token symbol">content_type</span><span class="token operator">:</span> <span class="token keyword">nil</span><span class="token punctuation">,</span> <span class="token symbol">metadata</span><span class="token operator">:</span> <span class="token keyword">nil</span><span class="token punctuation">)</span></span>
<span class="line">  create<span class="token operator">!</span> filename<span class="token operator">:</span> filename<span class="token punctuation">,</span> <span class="token symbol">byte_size</span><span class="token operator">:</span> byte_size<span class="token punctuation">,</span> <span class="token symbol">checksum</span><span class="token operator">:</span> checksum<span class="token punctuation">,</span> <span class="token symbol">content_type</span><span class="token operator">:</span> content_type<span class="token punctuation">,</span> <span class="token symbol">metadata</span><span class="token operator">:</span> metadata</span>
<span class="line"><span class="token keyword">end</span></span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>我們發現建立一個 Blob 至少需要 <code>filename</code>、<code>byte_size</code>、<code>checksum</code>，而這三個除了 <code>checksum</code>，其他我們都可以透過 S3 <code>get_object</code> 拿到資料，而 <code>checksum</code> 我們則需要實作計算的方式，我是直接 copy Blob 內的 private 方法 <code>compute_checksum_in_chunks</code> 來實作。</p>
<p>接下來讓我們自己來實作建立一個檔案已經存放在 S3 的 Blob 吧：</p>
<div class="language-ruby line-numbers-mode" data-highlighter="prismjs" data-ext="rb"><pre><code class="language-ruby"><span class="line"><span class="token keyword">def</span> <span class="token method-definition"><span class="token function">compute_checksum_in_chunks</span></span><span class="token punctuation">(</span>io<span class="token punctuation">)</span></span>
<span class="line">  Digest<span class="token double-colon punctuation">::</span><span class="token class-name">MD5</span><span class="token punctuation">.</span><span class="token keyword">new</span><span class="token punctuation">.</span>tap <span class="token keyword">do</span> <span class="token operator">|</span>checksum<span class="token operator">|</span></span>
<span class="line">    <span class="token keyword">while</span> chunk <span class="token operator">=</span> io<span class="token punctuation">.</span>read<span class="token punctuation">(</span><span class="token number">5.</span>megabytes<span class="token punctuation">)</span></span>
<span class="line">      checksum <span class="token operator">&lt;&lt;</span> chunk</span>
<span class="line">    <span class="token keyword">end</span></span>
<span class="line"></span>
<span class="line">    io<span class="token punctuation">.</span>rewind</span>
<span class="line">  <span class="token keyword">end</span><span class="token punctuation">.</span>base64digest</span>
<span class="line"><span class="token keyword">end</span></span>
<span class="line"></span>
<span class="line"><span class="token keyword">def</span> <span class="token method-definition"><span class="token function">create_blob</span></span><span class="token punctuation">(</span>s3_key<span class="token punctuation">)</span></span>
<span class="line">  s3 <span class="token operator">=</span> Aws<span class="token double-colon punctuation">::</span><span class="token constant">S3</span><span class="token double-colon punctuation">::</span><span class="token class-name">Client</span><span class="token punctuation">.</span><span class="token keyword">new</span><span class="token punctuation">(</span><span class="token symbol">region</span><span class="token operator">:</span> <span class="token string-literal"><span class="token string">'us-west-1'</span></span><span class="token punctuation">)</span></span>
<span class="line">  converted_file <span class="token operator">=</span> s3<span class="token punctuation">.</span>get_object<span class="token punctuation">(</span><span class="token punctuation">{</span><span class="token symbol">bucket</span><span class="token operator">:</span> <span class="token string-literal"><span class="token string">'example-bucket'</span></span><span class="token punctuation">,</span> <span class="token symbol">key</span><span class="token operator">:</span> s3_key<span class="token punctuation">}</span><span class="token punctuation">)</span></span>
<span class="line"></span>
<span class="line">  blob_params <span class="token operator">=</span> <span class="token punctuation">{</span></span>
<span class="line">    <span class="token symbol">filename</span><span class="token operator">:</span> <span class="token string-literal"><span class="token string">"change_your_filename.pdf"</span></span><span class="token punctuation">,</span></span>
<span class="line">    <span class="token symbol">content_type</span><span class="token operator">:</span> converted_file<span class="token punctuation">.</span>content_type<span class="token punctuation">,</span></span>
<span class="line">    <span class="token symbol">byte_size</span><span class="token operator">:</span> converted_file<span class="token punctuation">.</span>content_length<span class="token punctuation">,</span></span>
<span class="line">    <span class="token symbol">checksum</span><span class="token operator">:</span> compute_checksum_in_chunks<span class="token punctuation">(</span>converted_file<span class="token punctuation">.</span>body<span class="token punctuation">)</span></span>
<span class="line">  <span class="token punctuation">}</span></span>
<span class="line"></span>
<span class="line">  blob <span class="token operator">=</span> ActiveStorage<span class="token double-colon punctuation">::</span>Blob<span class="token punctuation">.</span>create_before_direct_upload<span class="token operator">!</span><span class="token punctuation">(</span>blob_params<span class="token punctuation">)</span></span>
<span class="line">  blob<span class="token punctuation">.</span>update_attributes key<span class="token symbol">:s3_key</span></span>
<span class="line"></span>
<span class="line">  <span class="token keyword">return</span> blob</span>
<span class="line"><span class="token keyword">end</span></span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>這邊有一點值得注意的是：建立一個 Blob 時，Blob 會自己產生一個 <code>key</code>，但這組 <code>key</code> 並不是 S3 上檔案的 <code>key</code>，因此我們需要透過 <code>update_attributes</code> 去改 <code>key</code> 參數。</p>
<p>關於 key 的實作，可以看一下 Blob 內的實作：</p>
<p><a href="https://github.com/rails/rails/blob/v5.2.0/activestorage/app/models/active_storage/blob.rb#L83" target="_blank" rel="noopener noreferrer">activestorage/app/models/active_storage/blob.rb#L83</a></p>
<div class="language-ruby line-numbers-mode" data-highlighter="prismjs" data-ext="rb"><pre><code class="language-ruby"><span class="line"><span class="token comment"># Returns the key pointing to the file on the service that's associated with this blob. The key is in the</span></span>
<span class="line"><span class="token comment"># standard secure-token format from Rails. So it'll look like: XTAPjJCJiuDrLk3TmwyJGpUo. This key is not intended</span></span>
<span class="line"><span class="token comment"># to be revealed directly to the user. Always refer to blobs using the signed_id or a verified form of the key.</span></span>
<span class="line"><span class="token keyword">def</span> <span class="token method-definition"><span class="token function">key</span></span></span>
<span class="line">  <span class="token comment"># We can't wait until the record is first saved to have a key for it</span></span>
<span class="line">  <span class="token keyword">self</span><span class="token punctuation">[</span><span class="token symbol">:key</span><span class="token punctuation">]</span> <span class="token operator">||=</span> <span class="token keyword">self</span><span class="token punctuation">.</span><span class="token keyword">class</span><span class="token punctuation">.</span>generate_unique_secure_token</span>
<span class="line"><span class="token keyword">end</span></span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>到這裡為止，我們已經可以透過 <code>create_blob</code> 這個方法丟入 <code>s3_key</code> 給他，然後我們會得到建立好的 Blob，這時候我們可以直接更新 ActiveStorage 設定的那個欄位（這裡以 <code>has_one_attached</code> 的欄位 <code>converted_file</code> 做示範）。</p>
<div class="language-ruby line-numbers-mode" data-highlighter="prismjs" data-ext="rb"><pre><code class="language-ruby"><span class="line">converted_blob <span class="token operator">=</span> create_blob<span class="token punctuation">(</span><span class="token string-literal"><span class="token string">'converted/example_s3_file_key'</span></span><span class="token punctuation">)</span></span>
<span class="line"></span>
<span class="line">user <span class="token operator">=</span> User<span class="token punctuation">.</span>find<span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">)</span></span>
<span class="line"><span class="token comment"># 直接餵 blob</span></span>
<span class="line">user<span class="token punctuation">.</span>update<span class="token operator">!</span><span class="token punctuation">(</span><span class="token symbol">converted_file</span><span class="token operator">:</span> converted_blob<span class="token punctuation">)</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># 或者餵 blob 的 signed_id</span></span>
<span class="line">user<span class="token punctuation">.</span>update<span class="token operator">!</span><span class="token punctuation">(</span><span class="token symbol">converted_file</span><span class="token operator">:</span> converted_blob<span class="token punctuation">.</span>signed_id<span class="token punctuation">)</span></span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>這樣就實作完了，把已經在 S3 上的檔案，關聯回 Rails 的 record 了，至於 <code>update</code> 時要餵 Blob 或 Blob 的 <code>signed_id</code> 其實都可以，這邊會特別拿出來說明是因為當時 <a href="https://stackoverflow.com/questions/52323977/rails-activestorage-attachment-to-existing-s3-file" target="_blank" rel="noopener noreferrer">StackOverflow 的範例</a>只有 signed_id，困惑了我很久，不過這裡有找到相關的原始碼（此處以 <code>has_one_attached</code> 來說明）：</p>
<p><a href="https://github.com/rails/rails/blob/v5.2.0/activestorage/lib/active_storage/attached/one.rb#L16" target="_blank" rel="noopener noreferrer">activestorage/lib/active_storage/attached/one.rb#L16</a></p>
<div class="language-ruby line-numbers-mode" data-highlighter="prismjs" data-ext="rb"><pre><code class="language-ruby"><span class="line"><span class="token keyword">def</span> <span class="token method-definition"><span class="token function">attach</span></span><span class="token punctuation">(</span>attachable<span class="token punctuation">)</span></span>
<span class="line">  blob_was <span class="token operator">=</span> blob <span class="token keyword">if</span> attached<span class="token operator">?</span></span>
<span class="line">  blob <span class="token operator">=</span> create_blob_from<span class="token punctuation">(</span>attachable<span class="token punctuation">)</span></span>
<span class="line"></span>
<span class="line">  <span class="token operator">...</span></span>
<span class="line"><span class="token keyword">end</span></span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><a href="https://github.com/rails/rails/blob/v5.2.0/activestorage/lib/active_storage/attached.rb#L18" target="_blank" rel="noopener noreferrer">activestorage/lib/active_storage/attached.rb#L18</a></p>
<div class="language-ruby line-numbers-mode" data-highlighter="prismjs" data-ext="rb"><pre><code class="language-ruby"><span class="line"><span class="token keyword">def</span> <span class="token method-definition"><span class="token function">create_blob_from</span></span><span class="token punctuation">(</span>attachable<span class="token punctuation">)</span></span>
<span class="line">  <span class="token keyword">case</span> attachable</span>
<span class="line">  <span class="token keyword">when</span> ActiveStorage<span class="token double-colon punctuation">::</span>Blob</span>
<span class="line">    attachable</span>
<span class="line">  <span class="token keyword">when</span> ActionDispatch<span class="token double-colon punctuation">::</span>Http<span class="token double-colon punctuation">::</span>UploadedFile<span class="token punctuation">,</span> Rack<span class="token double-colon punctuation">::</span>Test<span class="token double-colon punctuation">::</span>UploadedFile</span>
<span class="line">    ActiveStorage<span class="token double-colon punctuation">::</span>Blob<span class="token punctuation">.</span>create_after_upload<span class="token operator">!</span> \</span>
<span class="line">      <span class="token symbol">io</span><span class="token operator">:</span> attachable<span class="token punctuation">.</span>open<span class="token punctuation">,</span></span>
<span class="line">      <span class="token symbol">filename</span><span class="token operator">:</span> attachable<span class="token punctuation">.</span>original_filename<span class="token punctuation">,</span></span>
<span class="line">      <span class="token symbol">content_type</span><span class="token operator">:</span> attachable<span class="token punctuation">.</span>content_type</span>
<span class="line">  <span class="token keyword">when</span> <span class="token builtin">Hash</span></span>
<span class="line">    ActiveStorage<span class="token double-colon punctuation">::</span>Blob<span class="token punctuation">.</span>create_after_upload<span class="token operator">!</span><span class="token punctuation">(</span>attachable<span class="token punctuation">)</span></span>
<span class="line">  <span class="token keyword">when</span> <span class="token builtin">String</span></span>
<span class="line">    ActiveStorage<span class="token double-colon punctuation">::</span>Blob<span class="token punctuation">.</span>find_signed<span class="token punctuation">(</span>attachable<span class="token punctuation">)</span></span>
<span class="line">  <span class="token keyword">else</span></span>
<span class="line">    <span class="token keyword">nil</span></span>
<span class="line">  <span class="token keyword">end</span></span>
<span class="line"><span class="token keyword">end</span></span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>這裡可發現你餵的 <code>attachable</code> 會檢查是什麼，如果是 <code>String</code> 則會以 <code>signed_id</code> 去做存取，如果是 <code>Blob</code> 則會直接寫 attachable，註解表示可以餵 4 種類型的參數：</p>
<div class="language-ruby line-numbers-mode" data-highlighter="prismjs" data-ext="rb"><pre><code class="language-ruby"><span class="line"><span class="token comment">#   person.avatar.attach(params[:avatar]) # ActionDispatch::Http::UploadedFile object</span></span>
<span class="line"><span class="token comment">#   person.avatar.attach(params[:signed_blob_id]) # Signed reference to blob from direct upload</span></span>
<span class="line"><span class="token comment">#   person.avatar.attach(io: File.open("/path/to/face.jpg"), filename: "face.jpg", content_type: "image/jpg")</span></span>
<span class="line"><span class="token comment">#   person.avatar.attach(avatar_blob) # ActiveStorage::Blob object</span></span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>不過我並沒有去探究 <code>update!</code> 方法到 <code>attach</code> 中間過程的程式碼跟實現方式，這裡我並沒有辦法很確定是不是直接關聯到這，但 <code>update!</code> 存取時餵 <code>Blob</code> 或 <code>Blob 的 signed_id</code> 我測試過是可行的。</p>
<h2>參考資料</h2>
<ul>
<li><a href="https://stackoverflow.com/questions/52323977/rails-activestorage-attachment-to-existing-s3-file" target="_blank" rel="noopener noreferrer">Rails ActiveStorage attachment to existing S3 file</a></li>
<li><a href="https://github.com/rails/rails/tree/v5.2.0/activestorage" target="_blank" rel="noopener noreferrer">Rails ActiveStorage</a></li>
</ul>
]]></content:encoded>
    </item>
    <item>
      <title>Logstash JDBC input timezone 問題</title>
      <link>https://blog.giiin.dev/posts/ELK/2020/120701.html</link>
      <guid>https://blog.giiin.dev/posts/ELK/2020/120701.html</guid>
      <source url="https://blog.giiin.dev/rss.xml">Logstash JDBC input timezone 問題</source>
      <description>Summary
之前透過 Logstash 的 JDBC input 把資料從 MySQL 匯入到 Elasticsearch，結果發生某個 datetime 欄位的時間竟然差了約 6 個小時，一開始覺得很奇怪，因為存在資料庫的時間都是 UTC +00:00，而 Rails 存進去之前都會統一使用 UTC +00:00 進行存取，只有在 Applica...</description>
      <category>ELK</category>
      <pubDate>Mon, 07 Dec 2020 00:00:00 GMT</pubDate>
      <content:encoded><![CDATA[<h2>Summary</h2>
<p>之前透過 Logstash 的 JDBC input 把資料從 MySQL 匯入到 Elasticsearch，結果發生某個 datetime 欄位的時間竟然差了約 6 個小時，一開始覺得很奇怪，因為存在資料庫的時間都是 <code>UTC +00:00</code>，而 Rails 存進去之前都會統一使用 <code>UTC +00:00</code> 進行存取，只有在 Application 內會依照預設時區設定顯示，所以一直摸不著頭緒是哪邊造成的，後來發現 MySQL 內的時區是 <code>CST</code>，而 <code>CST</code> 時區本身有四種含義：</p>
<ol>
<li>Central Standard Time (標準時間：<code>UTC -06:00</code>、夏令時間：<code>UTC -05:00</code>)</li>
<li>Australia Central Standard Time (<code>UTC +09:30</code>）</li>
<li>China Standard Time (<code>UTC +08:00</code>)</li>
<li>Cuba Standard Time (<code>UTC -04:00</code>)</li>
</ol>
<p>而這原因可能導致原本是台灣時區 <code>UTC +08:00</code>，變成 <code>UTC -06:00</code>，不過我遇到的案例比較特別，是資料庫存的時間已經是 <code>UTC +00:00</code>，但 MySQL 的時區是設 <code>CST</code>，而 JDBC 在抓時當成 Central Standard Time 而造成時間相較資料庫早 6 hrs。</p>
<h2>解決辦法</h2>
<p>解決辦法有 2 種，一種是 JDBC Connection String 指定 Timezone，另一種是改 MySQL 時間</p>
<h3>JDBC Connection String 加上指定 UTC 時區</h3>
<div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">jdbc {</span>
<span class="line">  ...</span>
<span class="line">  jdbc_connection_string => "jdbc:mysql://${DB_HOST}/${DB_DATABASE}?useUnicode=true&amp;useJDBCCompliantTimezoneShift=true&amp;useLegacyDatetimeCode=false&amp;serverTimezone=UTC"</span>
<span class="line">  ...</span>
<span class="line">}</span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3>修改 MySQL default timezone</h3>
<ol>
<li>編輯 MySQL 檔案</li>
</ol>
<p><strong>Linux</strong></p>
<div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code class="language-bash"><span class="line"><span class="token function">sudo</span> <span class="token function">vi</span> /etc/mysql/mysql.conf.d/mysqld.cnf</span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div></div></div><p><strong>MacOS</strong></p>
<div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code class="language-bash"><span class="line"><span class="token function">vim</span> /usr/local/etc/my.cnf</span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div></div></div><ol start="2">
<li>設定 default timezone</li>
</ol>
<div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">[mysqld]</span>
<span class="line">...</span>
<span class="line">default-time-zone = "+00:00"</span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ol start="3">
<li>Restart MySQL</li>
</ol>
<h2>結論</h2>
<p>因為時區基本上會透過 Rails 轉成 UTC 存在 DB，所以我們最終選擇使用方法二，直接修改 MySQL default timezone，因為沒使用什麼時間函數，所以沒什麼影響，如果要直接改 DB 時間，可能需確認是否會影響 Application 本身。然後 sync 到 Elasticsearch 的資料，舊的有時間差的資料，我們就沒進行處理了，畢竟四個小時後就沒影響。</p>
]]></content:encoded>
    </item>
    <item>
      <title>wicked_pdf 最後一個單字會換行 &amp; 圖片出現白色遮罩</title>
      <link>https://blog.giiin.dev/posts/Ruby%20on%20Rails/2020/112201.html</link>
      <guid>https://blog.giiin.dev/posts/Ruby%20on%20Rails/2020/112201.html</guid>
      <source url="https://blog.giiin.dev/rss.xml">wicked_pdf 最後一個單字會換行 &amp; 圖片出現白色遮罩</source>
      <description>最近在開發一個功能是需要將 DB 的資料產成 PDF，於是使用了 wicked_pdf 在 Rails 上直接產生 PDF，wicked_pdf 安裝容易，使用簡單，底層是透過 wkhtmltopdf 進行將 html 轉換成 PDF，而 wkhtmltopdf 則使用到 Qt Webkit 進行 render，所以蠻容易遇到一些 CSS 造成跑版的問...</description>
      <category>Ruby on Rails</category>
      <pubDate>Sun, 22 Nov 2020 00:00:00 GMT</pubDate>
      <content:encoded><![CDATA[<p>最近在開發一個功能是需要將 DB 的資料產成 PDF，於是使用了 <a href="https://github.com/mileszs/wicked_pdf" target="_blank" rel="noopener noreferrer">wicked_pdf</a> 在 Rails 上直接產生 PDF，wicked_pdf 安裝容易，使用簡單，底層是透過 <a href="https://github.com/wkhtmltopdf/wkhtmltopdf" target="_blank" rel="noopener noreferrer">wkhtmltopdf</a> 進行將 html 轉換成 PDF，而 wkhtmltopdf 則使用到 Qt Webkit 進行 render，所以蠻容易遇到一些 CSS 造成跑版的問題。</p>
<p>今天遇到一個解了兩次，前後大概花了 2 天才解決，就是當使用 inline-block、inline-flex 等會遇到一行字中，最後一個英文單字會被換到第二行（查看 issue 好像連 float 都有），例如：<a href="https://github.com/wkhtmltopdf/wkhtmltopdf/issues/2505" target="_blank" rel="noopener noreferrer">wkhtmltopdf#2505</a> 這個 issue，一開始一直以為是 CSS 的問題，所以一直各種嘗試 <code>white-space: nowrap;</code> 等，但又會影響文字內容太多時無法換行的問題，在經過一番爬文後才發現不是 CSS 的問題。</p>
<p>其實 issue 裡面有很多人提到各種可能引發換行的原因及解法，主要有：</p>
<ol>
<li>字型 / fontsize</li>
<li>inline-block / inline-flex / float</li>
<li>freetype(https://github.com/wkhtmltopdf/wkhtmltopdf/issues/2505#issuecomment-286258695) (升級到特定版本，但這應該是 wkhtmltopdf 使用到的 Library，意味著你要自己重新 complie binary，不是很確定)</li>
<li>dpi</li>
<li>zoom</li>
</ol>
<p>後來我自己透過設定 dpi 參數解決了這個問題，不過仍須測試一陣子，有遇到相關問題的可以參考相關 issue：</p>
<ol>
<li><a href="https://github.com/wkhtmltopdf/wkhtmltopdf/issues/2505" target="_blank" rel="noopener noreferrer">wkhtmltopdf/wkhtmltopdf#2505</a></li>
<li><a href="https://github.com/wkhtmltopdf/wkhtmltopdf/issues/2184" target="_blank" rel="noopener noreferrer">wkhtmltopdf/wkhtmltopdf#2184</a></li>
<li><a href="https://github.com/wkhtmltopdf/wkhtmltopdf/issues/45" target="_blank" rel="noopener noreferrer">wkhtmltopdf/wkhtmltopdf#45</a></li>
<li><a href="https://github.com/jgm/pandoc/issues/4142" target="_blank" rel="noopener noreferrer">jgm/pandoc#4142</a></li>
</ol>
]]></content:encoded>
    </item>
    <item>
      <title>MySQL allow remote access</title>
      <link>https://blog.giiin.dev/posts/MySQL/2020/112202.html</link>
      <guid>https://blog.giiin.dev/posts/MySQL/2020/112202.html</guid>
      <source url="https://blog.giiin.dev/rss.xml">MySQL allow remote access</source>
      <description>Summary
前一陣子在使用 ELK，想把 MySQL 的資料 sync 到 ELK，需要把 MySQL 設為可以給外部連線，所以紀錄一下有哪些步驟。
Step 1. 編輯 MySQL config

Step 2. 編輯 bind-address 為 0.0.0.0

修改成

Restart MySQL

值為 127.0.0.1 表示僅 loc...</description>
      <category>MySQL</category>
      <pubDate>Thu, 20 Aug 2020 00:00:00 GMT</pubDate>
      <content:encoded><![CDATA[<h2>Summary</h2>
<p>前一陣子在使用 ELK，想把 MySQL 的資料 sync 到 ELK，需要把 MySQL 設為可以給外部連線，所以紀錄一下有哪些步驟。</p>
<h2>Step 1. 編輯 MySQL config</h2>
<div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code class="language-bash"><span class="line"><span class="token function">sudo</span> <span class="token function">vi</span> /etc/mysql/mysql.conf.d/mysqld.cnf</span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div></div></div><h2>Step 2. 編輯 bind-address 為 0.0.0.0</h2>
<div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">#</span>
<span class="line"># Instead of skip-networking the default is now to listen only on</span>
<span class="line"># localhost which is more compatible and is not less secure.</span>
<span class="line">bind-address            = 127.0.0.1</span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>修改成</p>
<div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">bind-address            = 0.0.0.0</span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div></div></div><p>Restart MySQL</p>
<div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code class="language-bash"><span class="line"><span class="token function">sudo</span> <span class="token function">service</span> mysql restart</span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div></div></div><p>值為 <code>127.0.0.1</code> 表示僅 localhost 可以進行連線，而如果設為 <code>0.0.0.0</code> 則允許外部連到 MySQL。</p>
<h2>Step 3. 新增 MySQL 使用者</h2>
<div class="language-sql line-numbers-mode" data-highlighter="prismjs" data-ext="sql"><pre><code class="language-sql"><span class="line"><span class="token keyword">CREATE</span> <span class="token keyword">USER</span> <span class="token string">'username'</span><span class="token variable">@'localhost'</span> IDENTIFIED <span class="token keyword">BY</span> <span class="token string">'user_password'</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div></div></div><ul>
<li><code>username</code>: 自行替換為你想要的使用者名稱</li>
<li><code>localhost</code>: 此處為 host，如果值為 <code>localhost</code> 表示僅允許這個使用者在這台機器上做連線，其他主機不行（SSH 除外），如果要外部連線，請設想連線主機的 IP，或也可以使用 <code>%</code> 代表任何主機都可以進行連線，請謹慎使用 <code>%</code>，盡量將權限、可連線的來源最小化。</li>
<li><code>user_password</code>: 使用者密碼</li>
</ul>
<h2>Step 4. Grant privileges</h2>
<div class="language-sql line-numbers-mode" data-highlighter="prismjs" data-ext="sql"><pre><code class="language-sql"><span class="line"><span class="token keyword">GRANT</span> <span class="token keyword">ALL</span> <span class="token keyword">PRIVILEGES</span> <span class="token keyword">ON</span> database_name<span class="token punctuation">.</span>table_name <span class="token keyword">TO</span> <span class="token string">'username'</span><span class="token variable">@'localhost'</span><span class="token punctuation">;</span></span>
<span class="line"></span>
<span class="line"><span class="token keyword">GRANT</span> <span class="token keyword">SELECT</span><span class="token punctuation">,</span> <span class="token keyword">INSERT</span><span class="token punctuation">,</span> <span class="token keyword">DELETE</span> <span class="token keyword">ON</span> database_name<span class="token punctuation">.</span><span class="token operator">*</span> <span class="token keyword">TO</span> <span class="token string">'username'</span><span class="token variable">@'localhost'</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>新增使用者的權限，僅允許 <code>SELECT</code>, <code>INSERT</code>, <code>DELETE</code>, <code>ALL PRIVILEGES</code> 等，並且可以指定資料庫及資料表，例如：我想新增使用者僅允許<strong>讀取</strong> <code>development</code> 資料庫的 <code>users</code> 資料表，則可以這樣設定：</p>
<div class="language-sql line-numbers-mode" data-highlighter="prismjs" data-ext="sql"><pre><code class="language-sql"><span class="line"><span class="token keyword">GRANT</span> <span class="token keyword">SELECT</span> <span class="token keyword">ON</span> development_<span class="token punctuation">.</span>users <span class="token keyword">TO</span> <span class="token string">'jay'</span><span class="token variable">@'localhost'</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div></div></div><p>Revoke privileges</p>
<div class="language-sql line-numbers-mode" data-highlighter="prismjs" data-ext="sql"><pre><code class="language-sql"><span class="line"><span class="token keyword">REVOKE</span> <span class="token keyword">ALL</span> <span class="token keyword">PRIVILEGES</span> <span class="token keyword">ON</span> database_name<span class="token punctuation">.</span><span class="token operator">*</span> <span class="token keyword">FROM</span> <span class="token string">'username'</span><span class="token variable">@'localhost'</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div></div></div><h2>Step 5. 遠端連線</h2>
<div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh"><pre><code class="language-bash"><span class="line">mysql <span class="token parameter variable">-u</span> username <span class="token parameter variable">-p</span> <span class="token parameter variable">-h</span> <span class="token string">'xxx.xxx.xxx.xxx'</span></span>
<span class="line"></span></code></pre>
<div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0"><div class="line-number"></div></div></div>]]></content:encoded>
    </item>
  </channel>
</rss>